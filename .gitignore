# ---------------------------------------------------------------
# NOT DEPLOYED — these carry client data and must stay off the web.
# Kept locally so the tools still work from this folder.
#   package-builder   : real pricing + studio inquiry address
#   video-corrections : posts to a live CRM endpoint
#   wedding-activities: Interac payment details + guest RSVP form
# The repo is public, so anything committed is public forever.
# Never remove these lines without deciding that deliberately.
# ---------------------------------------------------------------
tools/package-builder.html
tools/video-corrections.html
tools/wedding-activities.html

# Held back pending a decision: this page ships a Firebase config and talks
# to a Realtime Database that answers unauthenticated reads (verified 200 on
# GET /.json). It performs set/push/remove with no sign-in, so writes are
# likely open too. Lock the database rules down first, then delete this line.
tools/volley-bbq-zine.html

# Local version archive — snapshots duplicate ~1.8MB each and contain
# copies of the private tools above. Git is the shared history instead.
versions/

.DS_Store
