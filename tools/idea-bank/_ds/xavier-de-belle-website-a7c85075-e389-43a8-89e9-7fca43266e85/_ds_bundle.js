/* @ds-bundle: {"namespace":"XDB","components":[{"name":"ActionRow","sourcePath":"components/work/ActionRow/ActionRow.jsx"},{"name":"Band","sourcePath":"components/tools/Band/Band.jsx"},{"name":"Button","sourcePath":"components/actions/Button/Button.jsx"},{"name":"CardGrid","sourcePath":"components/tools/CardGrid/CardGrid.jsx"},{"name":"Chapter","sourcePath":"components/work/Chapter/Chapter.jsx"},{"name":"Chip","sourcePath":"components/actions/Chip/Chip.jsx"},{"name":"ClosingCall","sourcePath":"components/work/ClosingCall/ClosingCall.jsx"},{"name":"ControlButton","sourcePath":"components/actions/ControlButton/ControlButton.jsx"},{"name":"Display","sourcePath":"components/type/Display/Display.jsx"},{"name":"EmptyState","sourcePath":"components/tools/EmptyState/EmptyState.jsx"},{"name":"FilterRail","sourcePath":"components/tools/FilterRail/FilterRail.jsx"},{"name":"HudBrand","sourcePath":"components/map/HudBrand/HudBrand.jsx"},{"name":"HudControls","sourcePath":"components/map/HudControls/HudControls.jsx"},{"name":"HudHint","sourcePath":"components/map/HudHint/HudHint.jsx"},{"name":"HudShortcuts","sourcePath":"components/map/HudShortcuts/HudShortcuts.jsx"},{"name":"IndexPanel","sourcePath":"components/map/IndexPanel/IndexPanel.jsx"},{"name":"Kicker","sourcePath":"components/type/Kicker/Kicker.jsx"},{"name":"LogoMark","sourcePath":"components/nav/LogoMark/LogoMark.jsx"},{"name":"MapStage","sourcePath":"components/map/MapStage/MapStage.jsx"},{"name":"Marquee","sourcePath":"components/work/Marquee/Marquee.jsx"},{"name":"Masthead","sourcePath":"components/work/Masthead/Masthead.jsx"},{"name":"MindMap","sourcePath":"components/map/MindMap/MindMap.jsx"},{"name":"Root","sourcePath":"components/general/Root/Root.jsx"},{"name":"SiteFooter","sourcePath":"components/nav/SiteFooter/SiteFooter.jsx"},{"name":"SpecList","sourcePath":"components/work/SpecList/SpecList.jsx"},{"name":"ToolCard","sourcePath":"components/tools/ToolCard/ToolCard.jsx"},{"name":"Topbar","sourcePath":"components/nav/Topbar/Topbar.jsx"}],"sourceHashes":{"components/work/ActionRow/ActionRow.jsx":"061bdbb160be","components/work/ActionRow/ActionRow.d.ts":"b8f115555c97","components/work/ActionRow/ActionRow.prompt.md":"aa3a6bd4d217","components/tools/Band/Band.jsx":"950176bef7f6","components/tools/Band/Band.d.ts":"cb0d8a80591e","components/tools/Band/Band.prompt.md":"e2a0602dc79d","components/actions/Button/Button.jsx":"2d77ba448493","components/actions/Button/Button.d.ts":"479713a17459","components/actions/Button/Button.prompt.md":"001f760f3e68","components/tools/CardGrid/CardGrid.jsx":"05f14f56fd90","components/tools/CardGrid/CardGrid.d.ts":"952f5cc52ed9","components/tools/CardGrid/CardGrid.prompt.md":"4e2f7e69e498","components/work/Chapter/Chapter.jsx":"35942ddeec00","components/work/Chapter/Chapter.d.ts":"f3d1fb23f4b6","components/work/Chapter/Chapter.prompt.md":"b0aeac110c84","components/actions/Chip/Chip.jsx":"9cb06acedec6","components/actions/Chip/Chip.d.ts":"4fcc64fa182e","components/actions/Chip/Chip.prompt.md":"6f87db7421f5","components/work/ClosingCall/ClosingCall.jsx":"f837dc97712b","components/work/ClosingCall/ClosingCall.d.ts":"436c77b10d26","components/work/ClosingCall/ClosingCall.prompt.md":"de92b7db8365","components/actions/ControlButton/ControlButton.jsx":"65e7a4ff7272","components/actions/ControlButton/ControlButton.d.ts":"3aef01225630","components/actions/ControlButton/ControlButton.prompt.md":"035c85a42d71","components/type/Display/Display.jsx":"f65a711ea799","components/type/Display/Display.d.ts":"4827b673ee08","components/type/Display/Display.prompt.md":"1ef2cca209a8","components/tools/EmptyState/EmptyState.jsx":"10e11e226295","components/tools/EmptyState/EmptyState.d.ts":"11b13d8f509f","components/tools/EmptyState/EmptyState.prompt.md":"c90dce4c1222","components/tools/FilterRail/FilterRail.jsx":"abbfe6a0715b","components/tools/FilterRail/FilterRail.d.ts":"0e8ac320e71b","components/tools/FilterRail/FilterRail.prompt.md":"962a0569a890","components/map/HudBrand/HudBrand.jsx":"661e864e50d9","components/map/HudBrand/HudBrand.d.ts":"b399c924cc37","components/map/HudBrand/HudBrand.prompt.md":"7d7904135383","components/map/HudControls/HudControls.jsx":"247f1f5a2ae4","components/map/HudControls/HudControls.d.ts":"2ec190c286c8","components/map/HudControls/HudControls.prompt.md":"471953f8c140","components/map/HudHint/HudHint.jsx":"fac2eb686162","components/map/HudHint/HudHint.d.ts":"26af256eea8c","components/map/HudHint/HudHint.prompt.md":"067407595404","components/map/HudShortcuts/HudShortcuts.jsx":"cfe785fa4565","components/map/HudShortcuts/HudShortcuts.d.ts":"9fe4ed1baf43","components/map/HudShortcuts/HudShortcuts.prompt.md":"2e53aadc5323","components/map/IndexPanel/IndexPanel.jsx":"d25755496ef1","components/map/IndexPanel/IndexPanel.d.ts":"01fc5deedfe2","components/map/IndexPanel/IndexPanel.prompt.md":"d16c40a4daad","components/type/Kicker/Kicker.jsx":"45b46c604a3c","components/type/Kicker/Kicker.d.ts":"3d522d39a83d","components/type/Kicker/Kicker.prompt.md":"c90b2ff3e5c5","components/nav/LogoMark/LogoMark.jsx":"f283d2957bc8","components/nav/LogoMark/LogoMark.d.ts":"78f047395664","components/nav/LogoMark/LogoMark.prompt.md":"eda1fa91682c","components/map/MapStage/MapStage.jsx":"8ae2452ad785","components/map/MapStage/MapStage.d.ts":"616c8699bfc6","components/map/MapStage/MapStage.prompt.md":"5f447a8db0d7","components/work/Marquee/Marquee.jsx":"8e78a2246507","components/work/Marquee/Marquee.d.ts":"6b21e08cfc96","components/work/Marquee/Marquee.prompt.md":"3441582099c8","components/work/Masthead/Masthead.jsx":"6ea2e94c6363","components/work/Masthead/Masthead.d.ts":"0553982e6b7b","components/work/Masthead/Masthead.prompt.md":"f8bf67a177ae","components/map/MindMap/MindMap.jsx":"ca1f795fb309","components/map/MindMap/MindMap.d.ts":"8ba706be3731","components/map/MindMap/MindMap.prompt.md":"b307b29dfaf7","components/general/Root/Root.jsx":"0c16d5042f28","components/general/Root/Root.d.ts":"c8b4e3a59fc0","components/general/Root/Root.prompt.md":"4690bfdd8482","components/nav/SiteFooter/SiteFooter.jsx":"f6738d71d43c","components/nav/SiteFooter/SiteFooter.d.ts":"f78f2d5319cb","components/nav/SiteFooter/SiteFooter.prompt.md":"e2e9ed92e618","components/work/SpecList/SpecList.jsx":"87ad06847b51","components/work/SpecList/SpecList.d.ts":"13980f7071a1","components/work/SpecList/SpecList.prompt.md":"0144749ab7c3","components/tools/ToolCard/ToolCard.jsx":"026b95eaa205","components/tools/ToolCard/ToolCard.d.ts":"7596b1486a04","components/tools/ToolCard/ToolCard.prompt.md":"1b494991ee25","components/nav/Topbar/Topbar.jsx":"8b8094893514","components/nav/Topbar/Topbar.d.ts":"38a89c10c26c","components/nav/Topbar/Topbar.prompt.md":"57725847d937"},"inlinedExternals":[],"builtBy":"cc-design-sync"} */
"use strict";
var XDB = (() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __esm = (fn, res, err) => function __init() {
    if (err) throw err[0];
    try {
      return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
    } catch (e) {
      throw err = [e], e;
    }
  };
  var __commonJS = (cb, mod) => function __require() {
    try {
      return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
    } catch (e) {
      throw mod = 0, e;
    }
  };
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // <define:import.meta.env>
  var init_define_import_meta_env = __esm({
    "<define:import.meta.env>"() {
    }
  });

  // shim:react-shim
  var require_react_shim = __commonJS({
    "shim:react-shim"(exports, module) {
      init_define_import_meta_env();
      var R = window.React;
      function np(p, k) {
        var o = {};
        for (var x in p) if (x !== "children") o[x] = p[x];
        if (k !== void 0) o.key = k;
        return o;
      }
      function jsx28(t, p, k) {
        var c = p && p.children;
        return c === void 0 ? R.createElement(t, np(p, k)) : R.createElement(t, np(p, k), c);
      }
      function jsxs13(t, p, k) {
        return R.createElement.apply(R, [t, np(p, k)].concat(p.children));
      }
      module.exports = R;
      module.exports.jsx = jsx28;
      module.exports.jsxs = jsxs13;
      module.exports.jsxDEV = function(t, p, k, s) {
        return (s ? jsxs13 : jsx28)(t, p, k);
      };
      module.exports.Fragment = R.Fragment;
    }
  });

  // ds-kit/dist/index.es.js
  var index_es_exports = {};
  __export(index_es_exports, {
    ActionRow: () => ActionRow,
    Band: () => Band,
    Button: () => Button,
    CardGrid: () => CardGrid,
    Chapter: () => Chapter,
    Chip: () => Chip,
    ClosingCall: () => ClosingCall,
    ControlButton: () => ControlButton,
    Display: () => Display,
    EmptyState: () => EmptyState,
    FilterRail: () => FilterRail,
    HudBrand: () => HudBrand,
    HudControls: () => HudControls,
    HudHint: () => HudHint,
    HudShortcuts: () => HudShortcuts,
    IndexPanel: () => IndexPanel,
    Kicker: () => Kicker,
    LogoMark: () => LogoMark,
    MapStage: () => MapStage,
    Marquee: () => Marquee,
    Masthead: () => Masthead,
    MindMap: () => MindMap,
    Root: () => Root,
    SiteFooter: () => SiteFooter,
    SpecList: () => SpecList,
    ToolCard: () => ToolCard,
    Topbar: () => Topbar
  });
  init_define_import_meta_env();
  var import_jsx_runtime = __toESM(require_react_shim());
  var import_jsx_runtime2 = __toESM(require_react_shim());
  var import_jsx_runtime3 = __toESM(require_react_shim());
  var import_jsx_runtime4 = __toESM(require_react_shim());
  var import_jsx_runtime5 = __toESM(require_react_shim());
  var import_jsx_runtime6 = __toESM(require_react_shim());
  var import_jsx_runtime7 = __toESM(require_react_shim());
  var import_jsx_runtime8 = __toESM(require_react_shim());
  var import_jsx_runtime9 = __toESM(require_react_shim());
  var import_jsx_runtime10 = __toESM(require_react_shim());
  var React = __toESM(require_react_shim());
  var import_jsx_runtime11 = __toESM(require_react_shim());
  var import_jsx_runtime12 = __toESM(require_react_shim());
  var import_jsx_runtime13 = __toESM(require_react_shim());
  var import_jsx_runtime14 = __toESM(require_react_shim());
  var import_jsx_runtime15 = __toESM(require_react_shim());
  var import_jsx_runtime16 = __toESM(require_react_shim());
  var import_jsx_runtime17 = __toESM(require_react_shim());
  var import_jsx_runtime18 = __toESM(require_react_shim());
  var import_jsx_runtime19 = __toESM(require_react_shim());
  var import_jsx_runtime20 = __toESM(require_react_shim());
  var import_jsx_runtime21 = __toESM(require_react_shim());
  var import_jsx_runtime22 = __toESM(require_react_shim());
  var import_jsx_runtime23 = __toESM(require_react_shim());
  var import_jsx_runtime24 = __toESM(require_react_shim());
  var import_jsx_runtime25 = __toESM(require_react_shim());
  var import_jsx_runtime26 = __toESM(require_react_shim());
  var import_jsx_runtime27 = __toESM(require_react_shim());
  function Root({ children, surface = "ink", fullHeight = false, className, style }) {
    const cls = [
      "xdb-root",
      surface === "paper" ? "xdb-root--paper" : "",
      fullHeight ? "xdb-root--full" : "",
      className
    ].filter(Boolean).join(" ");
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: cls, style, children });
  }
  function Display({ children, as = "h2", tone = "paper", size = "inherit", className, id }) {
    const Tag = as;
    const cls = [
      "display",
      tone === "volt" ? "volt" : "",
      size !== "inherit" ? `display--${size}` : "",
      className
    ].filter(Boolean).join(" ");
    const style = tone === "stroke" ? { WebkitTextStroke: "2px var(--paper)", color: "transparent" } : void 0;
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(Tag, { className: cls, id, style, children });
  }
  function Kicker({ children, as = "p", className, style }) {
    const Tag = as;
    return /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(Tag, { className: ["kicker", className].filter(Boolean).join(" "), style, children });
  }
  function Button({
    children,
    href,
    variant = "outline",
    external,
    onClick,
    type = "button",
    className
  }) {
    const cls = ["btn", variant === "solid" ? "btn--solid" : "", className].filter(Boolean).join(" ");
    if (href) {
      return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
        "a",
        {
          className: cls,
          href,
          onClick,
          target: external ? "_blank" : void 0,
          rel: external ? "noopener" : void 0,
          children
        }
      );
    }
    return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("button", { className: cls, type, onClick, children });
  }
  function Chip({ children, pressed = false, onClick, className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
      "button",
      {
        type: "button",
        className: ["chip", className].filter(Boolean).join(" "),
        "aria-pressed": pressed,
        onClick,
        children
      }
    );
  }
  function ControlButton({
    children,
    wide = false,
    ariaLabel,
    onClick,
    className
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
      "button",
      {
        type: "button",
        className: ["ctl", wide ? "ctl--wide" : "", className].filter(Boolean).join(" "),
        "aria-label": ariaLabel,
        onClick,
        children
      }
    );
  }
  function LogoMark({ size = 26, className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)(
      "svg",
      {
        className,
        viewBox: "0 0 1571 1584",
        width: size,
        height: size,
        "aria-hidden": "true",
        focusable: "false",
        style: { display: "block" },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("rect", { width: "1571", height: "1584", fill: "black" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("polygon", { points: "184,81 555,81 749,693", fill: "white" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("polygon", { points: "1015,81 1386,81 821,693", fill: "white" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("polygon", { points: "184,1503 563,1503 762,876", fill: "white" }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("polygon", { points: "1007,1503 1386,1503 808,876", fill: "white" })
        ]
      }
    );
  }
  function Topbar({
    brand = "Xavier de Belle",
    brandHref = "index.html",
    links = [],
    className
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("header", { className: ["topbar", className].filter(Boolean).join(" "), children: [
      /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("a", { className: "topbar__mark", href: brandHref, children: [
        /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(LogoMark, {}),
        brand
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("nav", { className: "topbar__nav", "aria-label": "Main", children: links.map((l) => /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
        "a",
        {
          href: l.href,
          "aria-current": l.current ? "page" : void 0,
          target: l.external ? "_blank" : void 0,
          rel: l.external ? "noopener" : void 0,
          children: l.label
        },
        l.href + l.label
      )) })
    ] });
  }
  function SiteFooter({
    label = "Xavier de Belle \u2014 Montreal",
    links = [],
    signature,
    className
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("footer", { className: ["foot", className].filter(Boolean).join(" "), children: [
      /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(Kicker, { style: { marginBottom: 14 }, children: label }),
        /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("div", { className: "foot__links", children: links.map((l) => /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
          "a",
          {
            href: l.href,
            target: l.external ? "_blank" : void 0,
            rel: l.external ? "noopener" : void 0,
            children: l.label
          },
          l.href + l.label
        )) })
      ] }),
      signature ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("p", { className: "foot__sig", children: signature }) : null
    ] });
  }
  function MapStage({ children, height = "100vh", className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("div", { className: ["map-stage", className].filter(Boolean).join(" "), style: { height }, children });
  }
  function startMindMap(canvas, opts) {
    opts = opts || {};
    const _off = [];
    function on(target, type, fn, o) {
      if (!target) return;
      target.addEventListener(type, fn, o);
      _off.push(function() {
        target.removeEventListener(type, fn, o);
      });
    }
    function stageRect() {
      const r = canvas.getBoundingClientRect();
      return { w: Math.max(1, Math.round(r.width)), h: Math.max(1, Math.round(r.height)) };
    }
    const DEFAULT_MAP = [
      { label: "Work", children: [
        { label: "Real Estate", href: "work.html#real-estate" },
        { label: "333 Photo", href: "https://333photo.com/", external: true },
        { label: "De Belle Photo", href: "https://debellephotography.com/", external: true }
      ] },
      { label: "Tools", children: [
        { label: "Portfolio Makers", href: "tools.html#portfolio" },
        { label: "De Belle Tools", href: "tools.html#debelle" },
        { label: "Personal", href: "tools.html#personal" },
        { label: "Events", href: "tools.html#events" },
        { label: "Art", href: "tools.html#art" }
      ] },
      { label: "Connect", children: [
        { label: "Email", href: "mailto:xavier@debellephotography.com" },
        { label: "Instagram", href: "https://www.instagram.com/xavierdebelle/", external: true },
        { label: "Submit a Tool", href: "soon.html#submit-a-tool" }
      ] },
      { label: "About", children: [
        { label: "Who am I?", href: "soon.html#who-am-i" },
        { label: "Changelog", href: "changelog.html" },
        { label: "What is this website?", href: "soon.html#what-is-this" }
      ] },
      /* Distinct from Tools > Personal: that one is a category of builds, this is
         the part of the site that isn't work at all. Both keep their own label. */
      { label: "Personal", children: [
        { label: "Food for Thought", href: "soon.html#food-for-thought" },
        { label: "Digital Archives", href: "soon.html#digital-archives" },
        { label: "Self Improvement", href: "soon.html#self-improvement" }
      ] }
    ];
    const MAP = opts.map || DEFAULT_MAP;
    const GROW = {
      seed: 20260907,
      // change this and the whole map grows differently
      /* reference viewports the two arrangements are grown against -------------- */
      refPortrait: [420, 880],
      refLandscape: [1320, 780],
      /* NOTE: portrait rx tracks the anchor COUNT. A four-anchor square puts two
         anchors on the portrait's narrow axis, where they collide with the logo
         mark and need rx ~1.06. A five-anchor pentagon puts none there, so it can
         sit tighter. Re-check anchor clearance against the mark if this changes. */
      ring: { portrait: { rx: 0.78, ry: 0.82 }, landscape: { rx: 0.9, ry: 0.7 } },
      /* Two scales set everything, so both arrangements grow correctly:
           S        the layout scale — legibility is measured against this
           RING     the mean gap between neighbouring anchors — how much room a
                    cluster actually has, which on a wide landscape ring is much
                    more than S suggests.                                        */
      minNodeSep: 0.31,
      // ×S — labels stay legible; nothing closer than this
      sepAspect: [1.55, 0.72],
      // the separation test is elliptical, because a
      // label is wide and short: neighbours need more
      // room side by side than stacked.
      step: 0.3,
      // ×minNodeSep — branch step length
      killDist: 0.28,
      // ×minNodeSep — attractor consumed at this range
      influence: 1.25,
      // ×minNodeSep — how far an attractor reaches
      warpAmp: 0.3,
      // ×minNodeSep — the meander
      warpFreq: 1.35,
      // cycles across S
      rigidity: 0.52,
      // how much a branch keeps its previous heading
      wander: 0.22,
      // random jitter per step, radians
      maxKids: 3,
      // branchings allowed from one branch node
      iterCap: 300,
      /* growth regions — offset from the anchor, never centred on it ----------- */
      attractors: [32, 46, 32, 32, 32],
      // per cluster, sized to its fan.
      // Indexed BY CLUSTER, so this list must be re-sized
      // and re-ordered whenever the clusters change.
      // Tools carries five children and needs the most.
      regionOffset: 0.58,
      // region centre pushed this far along the growth dir
      regionR: [0.62, 0.58],
      // ×RING: along the growth dir, across it. Big
      // enough that neighbouring regions graze — the map
      // must not read as five tidy territories.
      regionNoise: 0.4,
      // raggedness of the region edge
      clumpBias: 2.6,
      // density skew toward the growth direction
      neighbourPush: 0.25,
      // how hard neighbours shove a growth direction away
      lean: 0.62,
      // ±radians of tangential lean per cluster. Some
      // neighbours end up leaning into each other, which
      // is where the territories bleed and cross.
      /* hanging the hierarchy on the branches ---------------------------------- */
      gapMin: 3,
      // a child sits at least this many branch steps out
      gapMax: 8,
      depthTaper: 0.84,
      // particle count / orbit multiplier per level down
      /* bleed ------------------------------------------------------------------ */
      crossLinks: 4,
      // links between nodes of neighbouring clusters
      crossDist: 2.4,
      // ×S — ceiling on how far a cross link may reach
      // in EITHER arrangement; pairs are then ranked by
      // their mean distance across the two.
      bundleHold: 0.55,
      // fraction of a shared trunk kept before splitting
      pathSpacing: 9
      // link paths resampled to this arc spacing (px)
    };
    const THEME = {
      dark: {
        bg: [0, 0, 0],
        particle: [255, 255, 255],
        // white; the greys come from alpha alone
        accent: [243, 201, 63],
        // hsl(46,88%,60%) — warm gold, not orange
        label: [236, 238, 242],
        labelDim: [176, 180, 190],
        hint: [150, 154, 164],
        alphaK: 1
        // particle alpha scale
      },
      light: {
        bg: [246, 246, 244],
        // near-white; pure #fff is harsh at this density
        particle: [8, 8, 10],
        accent: [176, 124, 6],
        // the same gold, darkened to hold on white
        label: [16, 17, 20],
        labelDim: [96, 100, 108],
        hint: [122, 126, 134],
        alphaK: 0.86
        // black on white bites harder — ease it off
      }
    };
    const T = {
      /* cloud ---------------------------------------------------------------- */
      countDesktop: 6e3,
      countMobile: 2500,
      cloudRadius: 560,
      // layout units (≈ CSS px at zoom 1)
      cloudSquash: [1.16, 0.94, 0.92],
      // x,y,z ellipsoid bias
      filamentShare: 0.64,
      // fraction of particles laid along strands
      fov: 1600,
      // perspective focal length
      /* rotation + drift ------------------------------------------------------ */
      yawSpeed: 0.03,
      // radians / second — slow, time-based only
      tiltBase: 0.2,
      tiltSwing: 0.11,
      tiltRate: 0.017,
      driftAmp: 13,
      // how far a free particle wanders from home
      driftRate: [0.035, 0.085],
      /* springs --------------------------------------------------------------- */
      stiffFree: [0.055, 0.105],
      stiffNode: [0.115, 0.215],
      dampFree: 0.86,
      dampNode: 0.8,
      /* bloom / release staggering -------------------------------------------- */
      bloomSpread: 0.85,
      // seconds between centre and rim starting
      bloomJitter: 0.3,
      releaseJitter: 0.4,
      /* recruitment sizes ----------------------------------------------------- */
      nAnchor: 76,
      // particles orbiting an anchor node
      nChild: 24,
      linkSpacing: 9.5,
      // one link particle per this much path length
      orbitAnchor: 17,
      // orbit radius, layout units
      orbitChild: 9.5,
      orbitFlatten: 0.45,
      // squash orbit in z so nodes read as discs
      linkJitter: 2.4,
      linkJitterZ: 4,
      /* breath ---------------------------------------------------------------- */
      breathPeriod: 7,
      // seconds, one full sine cycle
      breathBright: 0.26,
      // ± brightness swing
      breathOrbit: 0.11,
      // ± node orbit radius swing
      /* firing ---------------------------------------------------------------- */
      fireRate: 1.15,
      // base spontaneous firings per second at peak
      fireTravel: 0.62,
      // seconds for a wave to cross one link
      fireWidth: 0.3,
      // wave width in path-fraction
      fireNodeDecay: 0.85,
      // seconds a node stays lit after arrival
      cascadeChance: 0.55,
      // at breath peak, chance arrival spills onward
      /* negative space -------------------------------------------------------- */
      carveGrid: 64,
      carveAnchor: [104, 74, 50],
      // rx, ry, strength  (covers node + label)
      carveChild: [70, 46, 30],
      carveMax: 46,
      carveEase: 0.04,
      /* camera ---------------------------------------------------------------- */
      zoomRange: [0.42, 3.2],
      panLimit: 900,
      tapSlop: 7,
      // px of movement still counted as a tap
      /* theme ----------------------------------------------------------------- */
      doubleTap: 0.32,
      // seconds — window for a double tap on the mark
      themeEase: 0.075,
      // crossfade speed between dark and light
      /* first run ------------------------------------------------------------- */
      autoOpenAfter: 1.6
      // seconds — the map blooms on its own
    };
    const LAYOUT = {
      portrait: { rx: 0.74, ry: 0.9, childR: 0.48, spread: 1.72, sx: 0.44, sy: 0.44, cx: 620, cy: 520 },
      landscape: { rx: 0.95, ry: 0.8, childR: 0.48, spread: 1.48, sx: 0.44, sy: 0.44, cx: 640, cy: 520 }
    };
    const LOGO_SRC = [
      [[184, 81], [555, 81], [749, 693]],
      [[1015, 81], [1386, 81], [821, 693]],
      [[184, 1503], [563, 1503], [762, 876]],
      [[1007, 1503], [1386, 1503], [808, 876]]
    ];
    const LOGO = LOGO_SRC.map(function(tri) {
      return tri.map(function(p) {
        return [(p[0] - 785.5) / 1584, (p[1] - 792) / 1584];
      });
    });
    const TAU = Math.PI * 2;
    function mulberry32(a) {
      return function() {
        a |= 0;
        a = a + 1831565813 | 0;
        let t = Math.imul(a ^ a >>> 15, 1 | a);
        t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
        return ((t ^ t >>> 14) >>> 0) / 4294967296;
      };
    }
    const SIN_N = 2048, SIN_MASK = SIN_N - 1;
    const SINTAB = new Float32Array(SIN_N);
    for (let i = 0; i < SIN_N; i++) SINTAB[i] = Math.sin(i / SIN_N * TAU);
    function sinT(turns) {
      return SINTAB[turns * SIN_N & SIN_MASK];
    }
    function hashN(i, j, k) {
      let n = i * 1619 + j * 31337 + k * 6971 | 0;
      n = n << 13 ^ n;
      return 1 - (Math.imul(n, Math.imul(n, Math.imul(n, 15731)) + 789221) + 1376312589 & 2147483647) / 1073741824;
    }
    function smooth(t) {
      return t * t * (3 - 2 * t);
    }
    function vnoise(x, y, z) {
      const ix = Math.floor(x), iy = Math.floor(y), iz = Math.floor(z);
      const fx = smooth(x - ix), fy = smooth(y - iy), fz = smooth(z - iz);
      let a, b, c, d, e, f, g, h;
      a = hashN(ix, iy, iz);
      b = hashN(ix + 1, iy, iz);
      c = hashN(ix, iy + 1, iz);
      d = hashN(ix + 1, iy + 1, iz);
      e = hashN(ix, iy, iz + 1);
      f = hashN(ix + 1, iy, iz + 1);
      g = hashN(ix, iy + 1, iz + 1);
      h = hashN(ix + 1, iy + 1, iz + 1);
      const x1 = a + (b - a) * fx, x2 = c + (d - c) * fx, x3 = e + (f - e) * fx, x4 = g + (h - g) * fx;
      const y1 = x1 + (x2 - x1) * fy, y2 = x3 + (x4 - x3) * fy;
      return y1 + (y2 - y1) * fz;
    }
    function fbm(x, y, z) {
      let v = 0, amp = 0.55, fr = 1;
      for (let o = 0; o < 3; o++) {
        v += vnoise(x * fr, y * fr, z * fr) * amp;
        fr *= 2.07;
        amp *= 0.5;
      }
      return Math.max(0, Math.min(1, v * 0.5 + 0.5));
    }
    function clamp(v, a, b) {
      return v < a ? a : v > b ? b : v;
    }
    const cv = canvas;
    const ctx = cv.getContext("2d", { alpha: false, desynchronized: true });
    let vw = 0, vh = 0, cw = 0, ch = 0, dpr = 1, uiScale = 1, isPortrait = true, isSmall = false;
    const rmq = window.matchMedia ? window.matchMedia("(prefers-reduced-motion: reduce)") : null;
    let reduced = rmq ? rmq.matches : false;
    if (rmq && rmq.addEventListener) on(rmq, "change", function(e) {
      reduced = e.matches;
    });
    const ROLE_FREE = 0, ROLE_TAKEN = 1;
    let N = 0;
    let hx, hy, hz, px, py, pz, vx, vy, vz, ox, oy, oz, role, own, upos, stiff, damp, bri, szb, dphA, dphB, dphC, drt, twp, twr, t0, sxp, syp, ssz, bkt;
    function allocParticles(n) {
      N = n;
      const F = function() {
        return new Float32Array(n);
      };
      hx = F();
      hy = F();
      hz = F();
      px = F();
      py = F();
      pz = F();
      vx = F();
      vy = F();
      vz = F();
      ox = F();
      oy = F();
      oz = F();
      role = new Uint8Array(n);
      own = new Int32Array(n);
      upos = F();
      stiff = F();
      damp = F();
      bri = F();
      szb = F();
      dphA = F();
      dphB = F();
      dphC = F();
      drt = F();
      twp = F();
      twr = F();
      t0 = F();
      sxp = F();
      syp = F();
      ssz = F();
      bkt = new Int8Array(n);
    }
    const _sp = [0, 0, 0];
    function splineAt(cps, t) {
      const n = cps.length, f = t * (n - 1);
      let i = Math.min(n - 2, Math.floor(f));
      if (i < 0) i = 0;
      const u = f - i, u2 = u * u, u3 = u2 * u;
      const p0 = cps[Math.max(0, i - 1)], p1 = cps[i], p2 = cps[i + 1], p3 = cps[Math.min(n - 1, i + 2)];
      for (let d = 0; d < 3; d++) {
        _sp[d] = 0.5 * (2 * p1[d] + (-p0[d] + p2[d]) * u + (2 * p0[d] - 5 * p1[d] + 4 * p2[d] - p3[d]) * u2 + (-p0[d] + 3 * p1[d] - 3 * p2[d] + p3[d]) * u3);
      }
      return _sp;
    }
    function buildCloud() {
      const rnd = mulberry32(20260907);
      const g = function() {
        return (rnd() + rnd() + rnd() - 1.5) * 1.15;
      };
      const R = T.cloudRadius;
      const SQ = T.cloudSquash;
      let i = 0;
      const nFil = Math.max(12, Math.round(N / 210));
      const filEnd = Math.floor(N * T.filamentShare);
      const perFil = Math.floor(filEnd / nFil);
      for (let f = 0; f < nFil && i < filEnd; f++) {
        const cps = [];
        const a0 = rnd() * TAU, e0 = (rnd() - 0.5) * 1.5;
        let cxp = Math.cos(a0) * Math.cos(e0), cyp = Math.sin(e0), czp = Math.sin(a0) * Math.cos(e0);
        let rr2 = R * (0.14 + rnd() * 0.86);
        let X = cxp * rr2, Y = cyp * rr2 * 0.9, Z = czp * rr2;
        cps.push([X, Y, Z]);
        let dx = rnd() - 0.5, dy = (rnd() - 0.5) * 0.6, dz = rnd() - 0.5;
        for (let k = 0; k < 5; k++) {
          dx += (rnd() - 0.5) * 0.95;
          dy += (rnd() - 0.5) * 0.8;
          dz += (rnd() - 0.5) * 0.95;
          const L = Math.sqrt(dx * dx + dy * dy + dz * dz) || 1;
          const st = R * (0.3 + rnd() * 0.34);
          X += dx / L * st;
          Y += dy / L * st * 0.86;
          Z += dz / L * st;
          const rl = Math.sqrt(X * X + Y * Y + Z * Z);
          if (rl > R * 1.04) {
            const s = R * 1.04 / rl;
            X *= s;
            Y *= s;
            Z *= s;
          }
          cps.push([X, Y, Z]);
        }
        const thick = R * (0.016 + rnd() * 0.04);
        const wob = 1.9 + rnd() * 2.4, wph = rnd();
        for (let k = 0; k < perFil && i < filEnd; k++, i++) {
          const t = rnd();
          const p = splineAt(cps, t);
          const w = thick * (0.32 + 1.55 * Math.abs(sinT(t * wob + wph)));
          const g1 = g(), g2 = g(), g3 = g();
          hx[i] = (p[0] + g1 * w) * SQ[0];
          hy[i] = (p[1] + g2 * w) * SQ[1];
          hz[i] = (p[2] + g3 * w) * SQ[2];
          const core = Math.exp(-(g1 * g1 + g2 * g2 + g3 * g3) * 0.3);
          bri[i] = 0.3 + 0.7 * core * (0.58 + 0.42 * rnd());
          szb[i] = 0.82 + 0.6 * core;
        }
      }
      for (; i < N; i++) {
        let X = 0, Y = 0, Z = 0, ok = false;
        for (let tries = 0; tries < 16 && !ok; tries++) {
          let u, v, w, rl;
          do {
            u = rnd() * 2 - 1;
            v = rnd() * 2 - 1;
            w = rnd() * 2 - 1;
            rl = Math.sqrt(u * u + v * v + w * w);
          } while (rl > 1 || rl < 1e-4);
          const rad = Math.pow(rnd(), 0.44);
          X = u / rl * rad * R * SQ[0];
          Y = v / rl * rad * R * SQ[1];
          Z = w / rl * rad * R * SQ[2];
          const d = fbm(X * 56e-4, Y * 56e-4, Z * 56e-4);
          const radial = 1 - Math.pow(rad, 3.2) * 0.55;
          if (rnd() < d * d * radial * 2.6) ok = true;
        }
        hx[i] = X;
        hy[i] = Y;
        hz[i] = Z;
        bri[i] = 0.12 + 0.44 * rnd();
        szb[i] = 0.7 + 0.34 * rnd();
      }
      for (let k = 0; k < N; k++) {
        px[k] = hx[k];
        py[k] = hy[k];
        pz[k] = hz[k];
        role[k] = ROLE_FREE;
        own[k] = -1;
        t0[k] = 0;
        stiff[k] = T.stiffFree[0] + rnd() * (T.stiffFree[1] - T.stiffFree[0]);
        damp[k] = T.dampFree;
        dphA[k] = rnd();
        dphB[k] = rnd();
        dphC[k] = rnd();
        drt[k] = T.driftRate[0] + rnd() * (T.driftRate[1] - T.driftRate[0]);
        twp[k] = rnd();
        twr[k] = 0.06 + rnd() * 0.2;
      }
    }
    let cKX = 1, cKY = 1, cKZ = 1;
    function applyCloudScale(kx, ky, kz) {
      const rx = kx / cKX, ry = ky / cKY, rz = kz / cKZ;
      if (Math.abs(rx - 1) < 3e-3 && Math.abs(ry - 1) < 3e-3 && Math.abs(rz - 1) < 3e-3) return;
      for (let i = 0; i < N; i++) {
        hx[i] *= rx;
        hy[i] *= ry;
        hz[i] *= rz;
        if (role[i] === ROLE_FREE) {
          px[i] *= rx;
          py[i] *= ry;
          pz[i] *= rz;
        }
      }
      cKX = kx;
      cKY = ky;
      cKZ = kz;
    }
    const GROWN_BAKED = {
      cross: [9, 12, 13, 18, 2, 8, 4, 21],
      portrait: {
        p: [
          -0.043,
          -1.767,
          -0.057,
          -2.016,
          0.023,
          -2.263,
          -0.544,
          -1.842,
          0.721,
          -0.551,
          1.164,
          -0.666,
          1.028,
          -0.301,
          1.014,
          -0.921,
          1.177,
          -0.048,
          1.101,
          -1.219,
          0.481,
          1.434,
          0.762,
          1.636,
          0.714,
          1.946,
          1.214,
          2.116,
          -0.48,
          1.373,
          -0.443,
          1.693,
          -0.333,
          1.987,
          -0.819,
          1.866,
          -0.746,
          -0.522,
          -1.253,
          -0.469,
          -1.255,
          -0.727,
          -0.833,
          -0.254
        ],
        q: [
          20,
          0,
          0,
          -0.02,
          -0.146,
          -0.027,
          -0.242,
          -0.037,
          -0.332,
          -0.045,
          -0.414,
          -0.046,
          -0.494,
          -0.054,
          -0.569,
          -0.073,
          -0.641,
          -0.077,
          -0.717,
          -0.065,
          -0.801,
          -0.054,
          -0.895,
          -0.045,
          -0.996,
          -0.052,
          -1.097,
          -0.068,
          -1.203,
          -0.083,
          -1.314,
          -0.095,
          -1.418,
          -0.098,
          -1.52,
          -0.096,
          -1.62,
          -0.087,
          -1.717,
          -0.043,
          -1.767,
          4,
          -0.043,
          -1.767,
          -0.051,
          -1.856,
          -0.069,
          -1.936,
          -0.057,
          -2.016,
          7,
          -0.043,
          -1.767,
          -0.051,
          -1.856,
          -0.069,
          -1.936,
          -0.057,
          -2.016,
          -0.042,
          -2.102,
          -5e-3,
          -2.183,
          0.023,
          -2.263,
          8,
          -0.043,
          -1.767,
          -0.051,
          -1.856,
          -0.119,
          -1.905,
          -0.205,
          -1.928,
          -0.287,
          -1.918,
          -0.371,
          -1.9,
          -0.458,
          -1.866,
          -0.544,
          -1.842,
          11,
          0,
          0,
          0.053,
          -0.108,
          0.115,
          -0.16,
          0.179,
          -0.203,
          0.252,
          -0.244,
          0.326,
          -0.291,
          0.394,
          -0.343,
          0.463,
          -0.394,
          0.543,
          -0.445,
          0.626,
          -0.506,
          0.721,
          -0.551,
          6,
          0.721,
          -0.551,
          0.808,
          -0.567,
          0.894,
          -0.596,
          0.992,
          -0.628,
          1.073,
          -0.691,
          1.164,
          -0.666,
          6,
          0.721,
          -0.551,
          0.808,
          -0.567,
          0.9,
          -0.525,
          0.976,
          -0.46,
          0.998,
          -0.377,
          1.028,
          -0.301,
          7,
          0.721,
          -0.551,
          0.808,
          -0.567,
          0.894,
          -0.596,
          0.992,
          -0.628,
          1.035,
          -0.728,
          1.037,
          -0.831,
          1.014,
          -0.921,
          9,
          0.721,
          -0.551,
          0.808,
          -0.567,
          0.9,
          -0.525,
          0.976,
          -0.46,
          0.998,
          -0.377,
          1.028,
          -0.301,
          1.08,
          -0.22,
          1.125,
          -0.129,
          1.177,
          -0.048,
          8,
          0.721,
          -0.551,
          0.824,
          -0.605,
          0.901,
          -0.692,
          0.939,
          -0.802,
          1.017,
          -0.888,
          1.051,
          -1,
          1.103,
          -1.103,
          1.101,
          -1.219,
          17,
          0,
          0,
          9e-3,
          0.051,
          0.028,
          0.148,
          0.052,
          0.247,
          0.079,
          0.356,
          0.111,
          0.452,
          0.146,
          0.536,
          0.18,
          0.628,
          0.214,
          0.735,
          0.241,
          0.834,
          0.28,
          0.92,
          0.316,
          1.01,
          0.355,
          1.101,
          0.394,
          1.197,
          0.432,
          1.299,
          0.471,
          1.39,
          0.481,
          1.434,
          5,
          0.481,
          1.434,
          0.553,
          1.489,
          0.621,
          1.54,
          0.702,
          1.576,
          0.762,
          1.636,
          9,
          0.481,
          1.434,
          0.553,
          1.489,
          0.621,
          1.54,
          0.702,
          1.576,
          0.762,
          1.636,
          0.828,
          1.694,
          0.824,
          1.785,
          0.748,
          1.845,
          0.714,
          1.946,
          10,
          0.481,
          1.434,
          0.528,
          1.541,
          0.582,
          1.643,
          0.676,
          1.712,
          0.772,
          1.777,
          0.867,
          1.844,
          0.982,
          1.865,
          1.081,
          1.925,
          1.149,
          2.019,
          1.214,
          2.116,
          17,
          0,
          0,
          -0.05,
          0.043,
          -0.085,
          0.134,
          -0.12,
          0.227,
          -0.154,
          0.332,
          -0.193,
          0.433,
          -0.233,
          0.529,
          -0.257,
          0.627,
          -0.28,
          0.715,
          -0.316,
          0.802,
          -0.357,
          0.886,
          -0.384,
          0.971,
          -0.407,
          1.053,
          -0.439,
          1.123,
          -0.465,
          1.199,
          -0.481,
          1.278,
          -0.48,
          1.373,
          4,
          -0.48,
          1.373,
          -0.476,
          1.475,
          -0.468,
          1.586,
          -0.443,
          1.693,
          8,
          -0.48,
          1.373,
          -0.476,
          1.475,
          -0.468,
          1.586,
          -0.443,
          1.693,
          -0.457,
          1.784,
          -0.475,
          1.872,
          -0.419,
          1.945,
          -0.333,
          1.987,
          9,
          -0.48,
          1.373,
          -0.476,
          1.475,
          -0.468,
          1.586,
          -0.443,
          1.693,
          -0.457,
          1.784,
          -0.552,
          1.807,
          -0.648,
          1.824,
          -0.737,
          1.843,
          -0.819,
          1.866,
          11,
          0,
          0,
          -0.091,
          -0.096,
          -0.156,
          -0.148,
          -0.22,
          -0.199,
          -0.295,
          -0.244,
          -0.364,
          -0.286,
          -0.437,
          -0.324,
          -0.515,
          -0.358,
          -0.594,
          -0.397,
          -0.675,
          -0.448,
          -0.746,
          -0.522,
          7,
          -0.746,
          -0.522,
          -0.83,
          -0.538,
          -0.925,
          -0.572,
          -1.03,
          -0.585,
          -1.125,
          -0.608,
          -1.21,
          -0.558,
          -1.253,
          -0.469,
          7,
          -0.746,
          -0.522,
          -0.83,
          -0.538,
          -0.925,
          -0.572,
          -1.03,
          -0.585,
          -1.125,
          -0.608,
          -1.204,
          -0.653,
          -1.255,
          -0.727,
          4,
          -0.746,
          -0.522,
          -0.806,
          -0.447,
          -0.839,
          -0.353,
          -0.833,
          -0.254,
          20,
          1.177,
          -0.048,
          1.18,
          0.021,
          1.153,
          0.117,
          1.126,
          0.215,
          1.093,
          0.312,
          1.063,
          0.412,
          1.036,
          0.514,
          1.013,
          0.613,
          0.993,
          0.699,
          0.967,
          0.777,
          0.937,
          0.848,
          0.916,
          0.918,
          0.903,
          1.004,
          0.892,
          1.1,
          0.885,
          1.194,
          0.872,
          1.29,
          0.856,
          1.38,
          0.843,
          1.464,
          0.819,
          1.55,
          0.762,
          1.636,
          18,
          0.714,
          1.946,
          0.657,
          1.968,
          0.558,
          1.97,
          0.451,
          1.972,
          0.342,
          1.963,
          0.244,
          1.941,
          0.15,
          1.929,
          0.05,
          1.92,
          -0.046,
          1.913,
          -0.127,
          1.916,
          -0.207,
          1.92,
          -0.297,
          1.922,
          -0.389,
          1.915,
          -0.486,
          1.902,
          -0.581,
          1.891,
          -0.67,
          1.88,
          -0.758,
          1.868,
          -0.819,
          1.866,
          17,
          -0.057,
          -2.016,
          0.014,
          -1.981,
          0.068,
          -1.924,
          0.127,
          -1.847,
          0.193,
          -1.762,
          0.253,
          -1.689,
          0.32,
          -1.625,
          0.392,
          -1.564,
          0.465,
          -1.489,
          0.529,
          -1.405,
          0.589,
          -1.323,
          0.649,
          -1.248,
          0.721,
          -1.177,
          0.802,
          -1.117,
          0.874,
          -1.069,
          0.937,
          -1.008,
          1.014,
          -0.921,
          15,
          -0.544,
          -1.842,
          -0.576,
          -1.759,
          -0.622,
          -1.663,
          -0.667,
          -1.585,
          -0.719,
          -1.518,
          -0.779,
          -1.443,
          -0.838,
          -1.363,
          -0.889,
          -1.286,
          -0.939,
          -1.227,
          -0.991,
          -1.16,
          -1.049,
          -1.081,
          -1.105,
          -0.998,
          -1.17,
          -0.913,
          -1.227,
          -0.821,
          -1.255,
          -0.727
        ]
      },
      landscape: {
        p: [
          -0.062,
          -0.682,
          0.083,
          -0.923,
          0.108,
          -1.226,
          0.384,
          -1.427,
          1.465,
          -0.237,
          2.01,
          -0.22,
          1.906,
          -0.506,
          1.621,
          0.041,
          1.544,
          0.327,
          2.121,
          0.14,
          0.873,
          0.616,
          1.422,
          0.733,
          0.995,
          0.94,
          0.83,
          1.165,
          -0.926,
          0.551,
          -1.182,
          0.769,
          -1.414,
          1.021,
          -0.68,
          0.915,
          -1.504,
          -0.233,
          -1.979,
          -0.195,
          -1.581,
          -0.595,
          -1.482,
          -0.843
        ],
        q: [
          8,
          0,
          0,
          -0.027,
          -0.15,
          -0.04,
          -0.252,
          -0.057,
          -0.347,
          -0.07,
          -0.43,
          -0.079,
          -0.511,
          -0.097,
          -0.586,
          -0.062,
          -0.682,
          4,
          -0.062,
          -0.682,
          -0.036,
          -0.759,
          0.028,
          -0.835,
          0.083,
          -0.923,
          7,
          -0.062,
          -0.682,
          -0.036,
          -0.759,
          0.028,
          -0.835,
          0.083,
          -0.923,
          0.108,
          -1.023,
          0.113,
          -1.125,
          0.108,
          -1.226,
          9,
          -0.062,
          -0.682,
          -0.083,
          -0.796,
          -0.078,
          -0.912,
          -0.018,
          -1.012,
          0.055,
          -1.102,
          0.142,
          -1.179,
          0.209,
          -1.274,
          0.293,
          -1.354,
          0.384,
          -1.427,
          17,
          0,
          0,
          0.074,
          -0.06,
          0.161,
          -0.063,
          0.243,
          -0.065,
          0.329,
          -0.068,
          0.426,
          -0.078,
          0.52,
          -0.103,
          0.61,
          -0.122,
          0.702,
          -0.136,
          0.796,
          -0.151,
          0.896,
          -0.167,
          1.002,
          -0.184,
          1.101,
          -0.195,
          1.196,
          -0.211,
          1.292,
          -0.226,
          1.386,
          -0.24,
          1.465,
          -0.237,
          7,
          1.465,
          -0.237,
          1.557,
          -0.235,
          1.644,
          -0.256,
          1.737,
          -0.27,
          1.829,
          -0.261,
          1.923,
          -0.247,
          2.01,
          -0.22,
          8,
          1.465,
          -0.237,
          1.557,
          -0.235,
          1.644,
          -0.256,
          1.737,
          -0.27,
          1.829,
          -0.261,
          1.89,
          -0.325,
          1.911,
          -0.414,
          1.906,
          -0.506,
          5,
          1.465,
          -0.237,
          1.557,
          -0.235,
          1.616,
          -0.156,
          1.624,
          -0.058,
          1.621,
          0.041,
          8,
          1.465,
          -0.237,
          1.557,
          -0.235,
          1.616,
          -0.156,
          1.624,
          -0.058,
          1.621,
          0.041,
          1.566,
          0.128,
          1.554,
          0.227,
          1.544,
          0.327,
          8,
          1.465,
          -0.237,
          1.579,
          -0.256,
          1.693,
          -0.232,
          1.8,
          -0.186,
          1.89,
          -0.112,
          1.975,
          -0.033,
          2.067,
          0.038,
          2.121,
          0.14,
          12,
          0,
          0,
          0.059,
          0.019,
          0.125,
          0.089,
          0.199,
          0.156,
          0.283,
          0.219,
          0.357,
          0.284,
          0.437,
          0.35,
          0.524,
          0.409,
          0.605,
          0.475,
          0.68,
          0.551,
          0.76,
          0.62,
          0.873,
          0.616,
          7,
          0.873,
          0.616,
          0.925,
          0.691,
          1.004,
          0.74,
          1.106,
          0.758,
          1.209,
          0.762,
          1.316,
          0.741,
          1.422,
          0.733,
          4,
          0.873,
          0.616,
          0.93,
          0.718,
          0.952,
          0.832,
          0.995,
          0.94,
          6,
          0.873,
          0.616,
          0.92,
          0.723,
          0.919,
          0.839,
          0.928,
          0.955,
          0.886,
          1.064,
          0.83,
          1.165,
          13,
          0,
          0,
          -0.095,
          0,
          -0.167,
          0.055,
          -0.24,
          0.114,
          -0.321,
          0.163,
          -0.401,
          0.208,
          -0.476,
          0.261,
          -0.551,
          0.316,
          -0.635,
          0.363,
          -0.717,
          0.412,
          -0.795,
          0.457,
          -0.875,
          0.492,
          -0.926,
          0.551,
          5,
          -0.926,
          0.551,
          -0.972,
          0.631,
          -1.028,
          0.692,
          -1.101,
          0.736,
          -1.182,
          0.769,
          9,
          -0.926,
          0.551,
          -0.972,
          0.631,
          -1.028,
          0.692,
          -1.101,
          0.736,
          -1.182,
          0.769,
          -1.256,
          0.821,
          -1.317,
          0.881,
          -1.364,
          0.953,
          -1.414,
          1.021,
          6,
          -0.926,
          0.551,
          -0.909,
          0.636,
          -0.855,
          0.714,
          -0.808,
          0.789,
          -0.75,
          0.851,
          -0.68,
          0.915,
          17,
          0,
          0,
          -0.111,
          -0.059,
          -0.198,
          -0.066,
          -0.279,
          -0.072,
          -0.363,
          -0.077,
          -0.449,
          -0.078,
          -0.546,
          -0.083,
          -0.647,
          -0.087,
          -0.74,
          -0.092,
          -0.826,
          -0.097,
          -0.918,
          -0.104,
          -1.018,
          -0.126,
          -1.124,
          -0.155,
          -1.238,
          -0.184,
          -1.357,
          -0.212,
          -1.465,
          -0.232,
          -1.504,
          -0.233,
          7,
          -1.504,
          -0.233,
          -1.586,
          -0.255,
          -1.662,
          -0.264,
          -1.737,
          -0.266,
          -1.814,
          -0.261,
          -1.898,
          -0.234,
          -1.979,
          -0.195,
          6,
          -1.504,
          -0.233,
          -1.586,
          -0.255,
          -1.612,
          -0.331,
          -1.601,
          -0.413,
          -1.577,
          -0.5,
          -1.581,
          -0.595,
          9,
          -1.504,
          -0.233,
          -1.586,
          -0.255,
          -1.612,
          -0.331,
          -1.601,
          -0.413,
          -1.577,
          -0.5,
          -1.581,
          -0.595,
          -1.553,
          -0.681,
          -1.513,
          -0.764,
          -1.482,
          -0.843,
          6,
          1.544,
          0.327,
          1.556,
          0.443,
          1.532,
          0.518,
          1.508,
          0.586,
          1.49,
          0.662,
          1.422,
          0.733,
          19,
          0.995,
          0.94,
          0.881,
          0.929,
          0.784,
          0.93,
          0.686,
          0.933,
          0.597,
          0.945,
          0.513,
          0.949,
          0.424,
          0.949,
          0.329,
          0.951,
          0.223,
          0.957,
          0.112,
          0.969,
          0.015,
          0.977,
          -0.078,
          0.969,
          -0.174,
          0.963,
          -0.269,
          0.955,
          -0.364,
          0.948,
          -0.456,
          0.942,
          -0.543,
          0.923,
          -0.631,
          0.903,
          -0.68,
          0.915,
          21,
          0.083,
          -0.923,
          0.124,
          -0.847,
          0.195,
          -0.805,
          0.268,
          -0.765,
          0.347,
          -0.724,
          0.424,
          -0.691,
          0.507,
          -0.654,
          0.595,
          -0.609,
          0.679,
          -0.556,
          0.752,
          -0.504,
          0.828,
          -0.444,
          0.905,
          -0.389,
          0.975,
          -0.347,
          1.063,
          -0.306,
          1.159,
          -0.263,
          1.245,
          -0.219,
          1.327,
          -0.174,
          1.408,
          -0.123,
          1.489,
          -0.069,
          1.567,
          -0.012,
          1.621,
          0.041,
          24,
          0.384,
          -1.427,
          0.284,
          -1.413,
          0.192,
          -1.37,
          0.091,
          -1.334,
          -6e-3,
          -1.294,
          -0.087,
          -1.247,
          -0.162,
          -1.204,
          -0.237,
          -1.167,
          -0.31,
          -1.136,
          -0.387,
          -1.109,
          -0.463,
          -1.073,
          -0.544,
          -1.035,
          -0.63,
          -0.998,
          -0.72,
          -0.963,
          -0.803,
          -0.937,
          -0.886,
          -0.917,
          -0.981,
          -0.884,
          -1.079,
          -0.846,
          -1.173,
          -0.799,
          -1.254,
          -0.744,
          -1.335,
          -0.697,
          -1.421,
          -0.654,
          -1.512,
          -0.62,
          -1.581,
          -0.595
        ]
      }
    };
    function flattenMap() {
      const out = [{ label: "", cluster: -1, depth: 0, parent: -1 }];
      for (let c = 0; c < MAP.length; c++) {
        (function walk(node, parent, depth) {
          const i = out.length;
          out.push({
            label: node.label,
            cluster: c,
            depth,
            parent,
            url: node.url || "",
            external: !!node.external
          });
          const kids = node.children || [];
          for (let k = 0; k < kids.length; k++) walk(kids[k], i, depth + 1);
        })(MAP[c], 0, 1);
      }
      return out;
    }
    const FLAT = flattenMap();
    const KIDS = FLAT.map(function() {
      return [];
    });
    for (let i = 1; i < FLAT.length; i++) KIDS[FLAT[i].parent].push(i);
    const HEIGHT = new Int32Array(FLAT.length);
    for (let i = FLAT.length - 1; i >= 0; i--) {
      let h = 0;
      for (let k = 0; k < KIDS[i].length; k++) h = Math.max(h, HEIGHT[KIDS[i][k]] + 1);
      HEIGHT[i] = h;
    }
    const CLUSTER_ROOT = [];
    for (let i = 1; i < FLAT.length; i++) if (FLAT[i].depth === 1) CLUSTER_ROOT[FLAT[i].cluster] = i;
    const GL = {};
    function resolveLengths(S, ring) {
      const sep = GROW.minNodeSep * S;
      GL.minNodeSep = sep;
      GL.step = GROW.step * sep;
      GL.killDist = GROW.killDist * sep;
      GL.influence = GROW.influence * sep;
      GL.warpAmp = GROW.warpAmp * sep;
      GL.warpFreq = GROW.warpFreq / S;
      GL.regionRA = GROW.regionR[0] * ring;
      GL.regionRB = GROW.regionR[1] * ring;
      GL.ring = ring;
    }
    function warpPt(x, y, o) {
      const f = GL.warpFreq, a = GL.warpAmp;
      o[0] = x + (fbm(x * f, y * f, 0.17) * 2 - 1) * a;
      o[1] = y + (fbm(x * f + 53.1, y * f - 19.7, 4.83) * 2 - 1) * a;
    }
    function growSkeleton(rnd, A, D, nAtt) {
      const RA = GL.regionRA, RB = GL.regionRB;
      const cx = A[0] + D[0] * RA * GROW.regionOffset;
      const cy = A[1] + D[1] * RA * GROW.regionOffset;
      const qx = -D[1], qy = D[0];
      const ax = [], ay = [], dead = [];
      let guard = 0;
      while (ax.length < nAtt && guard++ < nAtt * 140) {
        const th = rnd() * TAU, rr0 = Math.sqrt(rnd());
        let u = Math.cos(th) * rr0, v = Math.sin(th) * rr0;
        const rag = 1 + (fbm(u * 3.1 + A[0] * 0.01, v * 3.1 + A[1] * 0.01, 2.7) * 2 - 1) * GROW.regionNoise;
        u *= rag;
        v *= rag;
        const X = cx + D[0] * u * RA + qx * v * RB;
        const Y = cy + D[1] * u * RA + qy * v * RB;
        const ex = X - A[0], ey = Y - A[1], el = Math.hypot(ex, ey) || 1;
        const align = (ex / el * D[0] + ey / el * D[1]) * 0.5 + 0.5;
        if (rnd() > Math.pow(align, GROW.clumpBias)) continue;
        ax.push(X);
        ay.push(Y);
        dead.push(0);
      }
      const t = { x: [A[0]], y: [A[1]], par: [-1], dx: [D[0]], dy: [D[1]], nk: [0] };
      const inf2 = GL.influence * GL.influence, kill2 = GL.killDist * GL.killDist;
      for (let it = 0; it < GROW.iterCap; it++) {
        const n = t.x.length;
        const sx = new Float64Array(n), sy = new Float64Array(n), sn = new Int32Array(n);
        let live = 0;
        for (let a = 0; a < ax.length; a++) {
          if (dead[a]) continue;
          live++;
          let best = -1, bd = inf2;
          for (let b = 0; b < n; b++) {
            const ex = ax[a] - t.x[b], ey = ay[a] - t.y[b], d = ex * ex + ey * ey;
            if (d < bd) {
              bd = d;
              best = b;
            }
          }
          if (best >= 0) {
            const ex = ax[a] - t.x[best], ey = ay[a] - t.y[best], el = Math.sqrt(bd) || 1;
            sx[best] += ex / el;
            sy[best] += ey / el;
            sn[best]++;
          }
        }
        if (!live) break;
        let grew = false;
        for (let b = 0; b < n; b++) {
          if (!sn[b] || t.nk[b] >= GROW.maxKids) continue;
          let ux = sx[b] / sn[b], uy = sy[b] / sn[b];
          ux = ux * (1 - GROW.rigidity) + t.dx[b] * GROW.rigidity;
          uy = uy * (1 - GROW.rigidity) + t.dy[b] * GROW.rigidity;
          const ang = Math.atan2(uy, ux) + (rnd() - 0.5) * 2 * GROW.wander;
          const cxx = Math.cos(ang), cyy = Math.sin(ang);
          t.x.push(t.x[b] + cxx * GL.step);
          t.y.push(t.y[b] + cyy * GL.step);
          t.par.push(b);
          t.dx.push(cxx);
          t.dy.push(cyy);
          t.nk.push(0);
          t.nk[b]++;
          grew = true;
        }
        if (!grew) break;
        for (let a = 0; a < ax.length; a++) {
          if (dead[a]) continue;
          for (let b = n; b < t.x.length; b++) {
            const ex = ax[a] - t.x[b], ey = ay[a] - t.y[b];
            if (ex * ex + ey * ey < kill2) {
              dead[a] = 1;
              break;
            }
          }
        }
      }
      t.kids = t.x.map(function() {
        return [];
      });
      for (let b = 1; b < t.par.length; b++) t.kids[t.par[b]].push(b);
      const o = [0, 0];
      for (let b = 0; b < t.x.length; b++) {
        warpPt(t.x[b], t.y[b], o);
        t.x[b] = o[0];
        t.y[b] = o[1];
      }
      return t;
    }
    function branchSteps(t, from, to) {
      let n = 0, c = to;
      while (c !== from && c >= 0) {
        c = t.par[c];
        n++;
        if (n > 400) return 1e9;
      }
      return c === from ? n : 1e9;
    }
    function branchDepthBelow(t, b) {
      let best = 0;
      const stack = [[b, 0]];
      while (stack.length) {
        const it = stack.pop();
        if (it[1] > best) best = it[1];
        const ks = t.kids[it[0]];
        for (let k = 0; k < ks.length; k++) stack.push([ks[k], it[1] + 1]);
      }
      return best;
    }
    function farEnough(x, y, placed, sep) {
      const kx = GROW.sepAspect[0], ky = GROW.sepAspect[1];
      const s2 = sep * sep;
      for (let k = 0; k < placed.length; k++) {
        const dx = (x - placed[k][0]) / kx, dy = (y - placed[k][1]) / ky;
        if (dx * dx + dy * dy < s2) return false;
      }
      return true;
    }
    function pushBranch(t, from, x, y, dx, dy) {
      t.x.push(x);
      t.y.push(y);
      t.par.push(from);
      t.dx.push(dx);
      t.dy.push(dy);
      t.nk.push(0);
      t.kids.push([]);
      t.kids[from].push(t.x.length - 1);
      t.nk[from]++;
      return t.x.length - 1;
    }
    function pickSlot(t, from, need, used, placed, corridors, rnd) {
      let best = -1, bestScore = -1e9;
      const seen = [[from, 0]];
      const visited = {};
      while (seen.length) {
        const it = seen.shift();
        const b = it[0], d = it[1];
        if (d > GROW.gapMax) continue;
        if (d >= GROW.gapMin && !used[b]) {
          const below = branchDepthBelow(t, b);
          if (below >= need * GROW.gapMin * 0.6 && farEnough(t.x[b], t.y[b], placed, GL.minNodeSep)) {
            let share = 0;
            for (let c = 0; c < corridors.length; c++) if (corridors[c][b]) share++;
            const score = below * 1 + share * GROW.bundleHold * 6 + d * 0.35 + rnd() * 1.6;
            if (score > bestScore) {
              bestScore = score;
              best = b;
            }
          }
        }
        const ks = t.kids[b];
        for (let k = 0; k < ks.length; k++) if (!visited[ks[k]]) {
          visited[ks[k]] = 1;
          seen.push([ks[k], d + 1]);
        }
      }
      if (best >= 0) return best;
      let cur = from;
      for (let tries = 0; tries < 26; tries++) {
        const ang = Math.atan2(t.dy[cur], t.dx[cur]) + (rnd() - 0.5) * 1;
        const nx = t.x[cur] + Math.cos(ang) * GL.step * 1.25;
        const ny = t.y[cur] + Math.sin(ang) * GL.step * 1.25;
        cur = pushBranch(t, cur, nx, ny, Math.cos(ang), Math.sin(ang));
        if (branchSteps(t, from, cur) >= GROW.gapMin && farEnough(nx, ny, placed, GL.minNodeSep)) return cur;
      }
      return cur;
    }
    function corridorOf(t, from, to) {
      const set = {};
      let c = to, n = 0;
      while (c !== from && c >= 0 && n++ < 400) {
        set[c] = 1;
        c = t.par[c];
      }
      return set;
    }
    function hangCluster(t, rootFlat, pos, bidx, placed, rnd) {
      const used = {};
      used[0] = 1;
      pos[rootFlat] = [t.x[0], t.y[0]];
      bidx[rootFlat] = 0;
      placed.push(pos[rootFlat]);
      (function walk(flatIdx, branchIdx) {
        const kids = KIDS[flatIdx].slice();
        kids.sort(function(a, b) {
          return HEIGHT[b] - HEIGHT[a];
        });
        const corridors = [];
        for (let k = 0; k < kids.length; k++) {
          const slot = pickSlot(t, branchIdx, HEIGHT[kids[k]], used, placed, corridors, rnd);
          used[slot] = 1;
          pos[kids[k]] = [t.x[slot], t.y[slot]];
          bidx[kids[k]] = slot;
          placed.push(pos[kids[k]]);
          corridors.push(corridorOf(t, branchIdx, slot));
          walk(kids[k], slot);
        }
      })(rootFlat, 0);
    }
    function subdivideWarp(ax, ay, bx, by) {
      const len = Math.hypot(bx - ax, by - ay);
      const n = Math.max(2, Math.round(len / GL.step) + 1);
      const out = [], o = [0, 0];
      for (let k = 0; k < n; k++) {
        const u = k / (n - 1);
        warpPt(ax + (bx - ax) * u, ay + (by - ay) * u, o);
        out.push(o[0], o[1]);
      }
      out[0] = ax;
      out[1] = ay;
      out[out.length - 2] = bx;
      out[out.length - 1] = by;
      return out;
    }
    function growLayout(ref, LCfg, ringCfg, rnd) {
      const Sx = Math.min(ref[0] * LCfg.sx, LCfg.cx);
      const Sy = Math.min(ref[1] * LCfg.sy, LCfg.cy);
      const S = Math.min(Sx, Sy);
      const anc = [];
      for (let c = 0; c < MAP.length; c++) {
        const a = -Math.PI / 2 + c * (TAU / MAP.length);
        anc.push([Math.cos(a) * ringCfg.rx * Sx, Math.sin(a) * ringCfg.ry * Sy]);
      }
      let ring = 0;
      for (let c = 0; c < MAP.length; c++) {
        const d = (c + 1) % MAP.length;
        ring += Math.hypot(anc[c][0] - anc[d][0], anc[c][1] - anc[d][1]);
      }
      ring /= MAP.length;
      resolveLengths(S, ring);
      const dir = [];
      for (let c = 0; c < MAP.length; c++) {
        let dx = anc[c][0], dy = anc[c][1];
        const dl = Math.hypot(dx, dy) || 1;
        dx /= dl;
        dy /= dl;
        for (let k = 0; k < MAP.length; k++) {
          if (k === c) continue;
          const ex = anc[c][0] - anc[k][0], ey = anc[c][1] - anc[k][1];
          const el = Math.hypot(ex, ey) || 1;
          const w = GROW.neighbourPush * (ring / el) * (ring / el);
          dx += ex / el * w;
          dy += ey / el * w;
        }
        const ang = Math.atan2(dy, dx) + (rnd() * 2 - 1) * GROW.lean;
        dir.push([Math.cos(ang), Math.sin(ang)]);
      }
      const pos = new Array(FLAT.length), bidx = new Array(FLAT.length);
      const trees = [], placed = [];
      pos[0] = [0, 0];
      for (let c = 0; c < MAP.length; c++) {
        const t = growSkeleton(rnd, anc[c], dir[c], GROW.attractors[c]);
        trees.push(t);
        hangCluster(t, CLUSTER_ROOT[c], pos, bidx, placed, rnd);
      }
      const paths = [];
      for (let j = 1; j < FLAT.length; j++) {
        const par = FLAT[j].parent;
        if (par === 0) {
          paths.push(subdivideWarp(0, 0, pos[j][0], pos[j][1]));
        } else {
          const t = trees[FLAT[j].cluster];
          const pts = [];
          let c = bidx[j];
          const stop2 = bidx[par];
          let guard = 0;
          while (c !== stop2 && c >= 0 && guard++ < 400) {
            pts.unshift(t.x[c], t.y[c]);
            c = t.par[c];
          }
          pts.unshift(t.x[stop2], t.y[stop2]);
          paths.push(pts);
        }
      }
      return { pos, paths, S, ring };
    }
    function r3(v) {
      return Math.round(v * 1e3) / 1e3;
    }
    function normDist(g, i, j) {
      return Math.hypot(g.pos[i][0] - g.pos[j][0], g.pos[i][1] - g.pos[j][1]) / g.S;
    }
    function bake(g, pairs) {
      resolveLengths(g.S, g.ring);
      const paths = g.paths.slice();
      for (let k = 0; k < pairs.length; k += 2)
        paths.push(subdivideWarp(
          g.pos[pairs[k]][0],
          g.pos[pairs[k]][1],
          g.pos[pairs[k + 1]][0],
          g.pos[pairs[k + 1]][1]
        ));
      const P = [], Q = [];
      for (let i = 1; i < FLAT.length; i++) P.push(r3(g.pos[i][0] / g.S), r3(g.pos[i][1] / g.S));
      for (let k = 0; k < paths.length; k++) {
        const a = paths[k];
        Q.push(a.length / 2);
        for (let m = 0; m < a.length; m++) Q.push(r3(a[m] / g.S));
      }
      return { p: P, q: Q };
    }
    function regrow() {
      const A = growLayout(
        GROW.refPortrait,
        LAYOUT.portrait,
        GROW.ring.portrait,
        mulberry32(GROW.seed)
      );
      const B = growLayout(
        GROW.refLandscape,
        LAYOUT.landscape,
        GROW.ring.landscape,
        mulberry32(GROW.seed ^ 376579)
      );
      const cand = [];
      for (let i = 1; i < FLAT.length; i++) {
        if (FLAT[i].depth < 2) continue;
        for (let j = i + 1; j < FLAT.length; j++) {
          if (FLAT[j].depth < 2 || FLAT[j].cluster === FLAT[i].cluster) continue;
          const da = normDist(A, i, j), db = normDist(B, i, j);
          if (Math.max(da, db) < GROW.crossDist) cand.push([(da + db) * 0.5, i, j]);
        }
      }
      cand.sort(function(a, b) {
        return a[0] - b[0];
      });
      const pairs = [], seen = {};
      for (let k = 0; k < cand.length && pairs.length < GROW.crossLinks * 2; k++) {
        const i = cand[k][1], j = cand[k][2];
        const key = FLAT[i].cluster + "-" + FLAT[j].cluster;
        if (seen[key] || seen[i] || seen[j]) continue;
        seen[key] = 1;
        seen[i] = 1;
        seen[j] = 1;
        pairs.push(i, j);
      }
      return { cross: pairs, portrait: bake(A, pairs), landscape: bake(B, pairs) };
    }
    let nodes = [], links = [];
    let nodeX, nodeY, ANC = [];
    let lPts = null, lOff = null, lN = null;
    let mapHalfW = 400, mapHalfH = 400;
    let ancHalfW = 400, ancHalfH = 400;
    let GROWN = GROWN_BAKED;
    function layoutStale() {
      return !GROWN || !GROWN.portrait || !GROWN.landscape || GROWN.portrait.p.length !== (FLAT.length - 1) * 2 || GROWN.landscape.p.length !== (FLAT.length - 1) * 2;
    }
    function resamplePath(raw, spacing) {
      const n = raw.length / 2;
      if (n < 2) {
        const x = raw[0] || 0, y = raw[1] || 0;
        return { pts: new Float32Array([x, y, x, y]), len: 1 };
      }
      const cum = new Float64Array(n);
      for (let k = 1; k < n; k++)
        cum[k] = cum[k - 1] + Math.hypot(raw[k * 2] - raw[k * 2 - 2], raw[k * 2 + 1] - raw[k * 2 - 1]);
      const len = cum[n - 1] || 1;
      const m = Math.max(2, Math.min(140, Math.round(len / spacing) + 1));
      const out = new Float32Array(m * 2);
      let seg = 0;
      for (let k = 0; k < m; k++) {
        const d = len * k / (m - 1);
        while (seg < n - 2 && cum[seg + 1] < d) seg++;
        const s0 = cum[seg], s1 = cum[seg + 1], t = s1 > s0 ? (d - s0) / (s1 - s0) : 0;
        out[k * 2] = raw[seg * 2] + (raw[seg * 2 + 2] - raw[seg * 2]) * t;
        out[k * 2 + 1] = raw[seg * 2 + 1] + (raw[seg * 2 + 3] - raw[seg * 2 + 1]) * t;
      }
      return { pts: out, len };
    }
    function pathAt(li, u, o) {
      const m = lN[li], f = u * (m - 1);
      let i = f | 0;
      if (i < 0) i = 0;
      else if (i > m - 2) i = m - 2;
      const t = f - i, b = (lOff[li] + i) * 2;
      o[0] = lPts[b] + (lPts[b + 2] - lPts[b]) * t;
      o[1] = lPts[b + 1] + (lPts[b + 3] - lPts[b + 1]) * t;
    }
    function pathTan(li, u, o) {
      const m = lN[li], f = u * (m - 1);
      let i = f | 0;
      if (i < 0) i = 0;
      else if (i > m - 2) i = m - 2;
      const b = (lOff[li] + i) * 2;
      const dx = lPts[b + 2] - lPts[b], dy = lPts[b + 3] - lPts[b + 1];
      const d = Math.hypot(dx, dy) || 1;
      o[0] = dx / d;
      o[1] = dy / d;
    }
    let geomBuilt = false;
    function buildStructure() {
      nodes = [{
        x: 0,
        y: 0,
        kind: 0,
        depth: 0,
        label: "",
        cluster: -1,
        parent: -1,
        url: "",
        external: false,
        hasKids: true,
        expanded: true,
        lox: 0,
        loy: 1,
        labelA: 0,
        arriveAt: 0,
        fireAt: -99,
        parts: []
      }];
      for (let i = 1; i < FLAT.length; i++) {
        const f = FLAT[i];
        nodes.push({
          x: 0,
          y: 0,
          kind: f.depth === 1 ? 1 : 2,
          depth: f.depth,
          label: f.label,
          cluster: f.cluster,
          parent: f.parent,
          url: f.url,
          external: f.external,
          hasKids: KIDS[i].length > 0,
          expanded: !isSmall,
          lox: 0,
          loy: 1,
          labelA: 0,
          arriveAt: 0,
          fireAt: -99,
          parts: []
        });
      }
      links = [];
      for (let i = 1; i < FLAT.length; i++)
        links.push({
          a: FLAT[i].parent,
          b: i,
          kind: FLAT[i].parent === 0 ? 0 : 1,
          cluster: FLAT[i].cluster,
          len: 1,
          parts: []
        });
      for (let k = 0; k < GROWN.cross.length; k += 2)
        links.push({
          a: GROWN.cross[k],
          b: GROWN.cross[k + 1],
          kind: 2,
          cluster: -1,
          len: 1,
          parts: []
        });
      ANC = [];
      for (let i = 1; i < nodes.length; i++) if (nodes[i].kind === 1) ANC[nodes[i].cluster] = i;
      nodeX = new Float32Array(nodes.length);
      nodeY = new Float32Array(nodes.length);
      lOff = new Int32Array(links.length);
      lN = new Int32Array(links.length);
      fireAt = new Float32Array(links.length).fill(-99);
      fireDone = new Uint8Array(links.length);
      geomBuilt = true;
    }
    function buildGeometry() {
      const L = isPortrait ? LAYOUT.portrait : LAYOUT.landscape;
      const Sx = Math.min(vw * L.sx, L.cx);
      const Sy = Math.min(vh * L.sy, L.cy);
      const S = Math.min(Sx, Sy);
      if (layoutStale()) GROWN = regrow();
      if (!geomBuilt) buildStructure();
      const G = isPortrait ? GROWN.portrait : GROWN.landscape;
      for (let i = 1; i < nodes.length; i++) {
        nodes[i].x = G.p[(i - 1) * 2] * S;
        nodes[i].y = G.p[(i - 1) * 2 + 1] * S;
      }
      for (let i = 1; i < nodes.length; i++) {
        const q = nodes[i].parent;
        const dx = nodes[i].x - nodes[q].x, dy = nodes[i].y - nodes[q].y;
        const d = Math.hypot(dx, dy) || 1;
        nodes[i].lox = dx / d;
        nodes[i].loy = dy / d;
      }
      const tmp = [];
      let r = 0, total = 0;
      for (let li = 0; li < links.length; li++) {
        const cnt = G.q[r++];
        const raw = new Float64Array(cnt * 2);
        for (let k = 0; k < cnt * 2; k++) raw[k] = G.q[r++] * S;
        const res = resamplePath(raw, GROW.pathSpacing * Math.max(0.7, uiScale));
        tmp.push(res);
        total += res.pts.length / 2;
      }
      if (!lPts || lPts.length < total * 2) lPts = new Float32Array(total * 2 + 512);
      let o = 0;
      for (let li = 0; li < links.length; li++) {
        lOff[li] = o / 2;
        lN[li] = tmp[li].pts.length / 2;
        links[li].len = tmp[li].len;
        const a = tmp[li].pts;
        for (let k = 0; k < a.length; k++) lPts[o++] = a[k];
      }
      syncGeometry();
      mapHalfW = 40;
      mapHalfH = 40;
      ancHalfW = 40;
      ancHalfH = 40;
      for (let i = 1; i < nodes.length; i++) {
        const anc = nodes[i].kind === 1;
        const ex = Math.abs(nodes[i].x) + (anc ? 58 : 76);
        const ey = Math.abs(nodes[i].y) + (anc ? 42 : 30);
        mapHalfW = Math.max(mapHalfW, ex);
        mapHalfH = Math.max(mapHalfH, ey);
        if (anc) {
          ancHalfW = Math.max(ancHalfW, ex);
          ancHalfH = Math.max(ancHalfH, ey);
        }
      }
    }
    function syncGeometry() {
      for (let i = 0; i < nodes.length; i++) {
        nodeX[i] = nodes[i].x;
        nodeY[i] = nodes[i].y;
      }
    }
    function nodeVisible(i) {
      if (!mapOpen) return false;
      const n = nodes[i];
      if (n.kind <= 1) return true;
      return nodes[ANC[n.cluster]].expanded;
    }
    function linkVisible(i) {
      if (!mapOpen) return false;
      const l = links[i];
      if (l.kind === 0) return true;
      if (l.kind === 2) return nodeVisible(l.a) && nodeVisible(l.b);
      return nodes[ANC[l.cluster]].expanded;
    }
    const CG = T.carveGrid;
    const carveU = new Float32Array(CG * CG);
    const carveV = new Float32Array(CG * CG);
    let cgX0 = 0, cgY0 = 0, cgSX = 1, cgSY = 1;
    let carveAmt = 0;
    function buildCarve() {
      const padX = 130, padY = 110;
      cgX0 = -mapHalfW - padX;
      cgY0 = -mapHalfH - padY;
      const spanX = (mapHalfW + padX) * 2, spanY = (mapHalfH + padY) * 2;
      cgSX = (CG - 1) / spanX;
      cgSY = (CG - 1) / spanY;
      const vis = [];
      for (let i = 1; i < nodes.length; i++) {
        if (!nodeVisible(i)) continue;
        const anc = nodes[i].kind === 1;
        const p = anc ? T.carveAnchor : T.carveChild;
        const k = anc ? 1 : Math.pow(GROW.depthTaper, nodes[i].depth - 2);
        vis.push([nodes[i].x, nodes[i].y, p[0] * uiScale * k, p[1] * uiScale * k, p[2] * uiScale * k]);
      }
      for (let gy = 0; gy < CG; gy++) {
        const wy = cgY0 + gy / cgSY;
        for (let gx = 0; gx < CG; gx++) {
          const wx = cgX0 + gx / cgSX;
          let ax = 0, ay = 0;
          for (let k = 0; k < vis.length; k++) {
            const n = vis[k];
            const dx = wx - n[0], dy = wy - n[1];
            const ex = dx / n[2], ey = dy / n[3];
            const d2 = ex * ex + ey * ey;
            if (d2 >= 1) continue;
            const f = (1 - d2) * (1 - d2);
            const d = Math.sqrt(dx * dx + dy * dy) + 1e-3;
            ax += dx / d * f * n[4];
            ay += dy / d * f * n[4];
          }
          const m = Math.sqrt(ax * ax + ay * ay);
          if (m > T.carveMax * uiScale) {
            const s = T.carveMax * uiScale / m;
            ax *= s;
            ay *= s;
          }
          const idx = gy * CG + gx;
          carveU[idx] = ax;
          carveV[idx] = ay;
        }
      }
    }
    const FG = 40;
    let fgStart, fgCount, fgItems, fgCur;
    let fgX0 = 0, fgY0 = 0, fgS = 1;
    function initFreeGrid() {
      fgStart = new Int32Array(FG * FG + 1);
      fgCount = new Int32Array(FG * FG);
      fgCur = new Int32Array(FG * FG);
      fgItems = new Int32Array(N);
    }
    function rebuildFreeGrid() {
      const R = T.cloudRadius * Math.max(cKX, cKY) * 1.35;
      fgX0 = -R;
      fgY0 = -R;
      fgS = (FG - 1) / (R * 2);
      fgCount.fill(0);
      const cell = function(x, y) {
        let gx = (x - fgX0) * fgS | 0, gy = (y - fgY0) * fgS | 0;
        if (gx < 0) gx = 0;
        else if (gx >= FG) gx = FG - 1;
        if (gy < 0) gy = 0;
        else if (gy >= FG) gy = FG - 1;
        return gy * FG + gx;
      };
      for (let i = 0; i < N; i++) {
        if (role[i] !== ROLE_FREE) continue;
        fgCount[cell(w2lX(i), w2lY(i))]++;
      }
      let acc = 0;
      for (let c = 0; c < FG * FG; c++) {
        fgStart[c] = acc;
        fgCur[c] = acc;
        acc += fgCount[c];
      }
      fgStart[FG * FG] = acc;
      for (let i = 0; i < N; i++) {
        if (role[i] !== ROLE_FREE) continue;
        fgItems[fgCur[cell(w2lX(i), w2lY(i))]++] = i;
      }
    }
    function takeNearestFree(lx, ly) {
      let gx = (lx - fgX0) * fgS | 0, gy = (ly - fgY0) * fgS | 0;
      if (gx < 0) gx = 0;
      else if (gx >= FG) gx = FG - 1;
      if (gy < 0) gy = 0;
      else if (gy >= FG) gy = FG - 1;
      let best = -1, bestCell = -1, bestSlot = -1, bestD = Infinity, foundRing = -1;
      for (let r = 0; r < FG; r++) {
        if (foundRing >= 0 && r > foundRing + 1) break;
        const x0 = gx - r, x1 = gx + r, y0 = gy - r, y1 = gy + r;
        for (let y = y0; y <= y1; y++) {
          if (y < 0 || y >= FG) continue;
          const edgeRow = y === y0 || y === y1;
          for (let x = x0; x <= x1; x++) {
            if (x < 0 || x >= FG) continue;
            if (!edgeRow && x !== x0 && x !== x1) continue;
            const c = y * FG + x, s2 = fgStart[c], n2 = fgCount[c];
            for (let k = 0; k < n2; k++) {
              const p = fgItems[s2 + k];
              const dx = w2lX(p) - lx, dy = w2lY(p) - ly;
              const d = dx * dx + dy * dy;
              if (d < bestD) {
                bestD = d;
                best = p;
                bestCell = c;
                bestSlot = k;
              }
            }
            if (n2 > 0 && foundRing < 0) foundRing = r;
          }
        }
      }
      if (best < 0) return -1;
      const s = fgStart[bestCell], n = fgCount[bestCell];
      fgItems[s + bestSlot] = fgItems[s + n - 1];
      fgCount[bestCell] = n - 1;
      return best;
    }
    let yaw = 0, tilt = T.tiltBase;
    let cyR = 1, syR = 0, ctR = 1, stR = 0;
    function updateRot() {
      cyR = Math.cos(yaw);
      syR = Math.sin(yaw);
      ctR = Math.cos(tilt);
      stR = Math.sin(tilt);
    }
    updateRot();
    function w2lX(i) {
      return px[i] * cyR + pz[i] * syR;
    }
    function w2lY(i) {
      return py[i] * ctR - (-px[i] * syR + pz[i] * cyR) * stR;
    }
    function w2lZ(i) {
      return py[i] * stR + (-px[i] * syR + pz[i] * cyR) * ctR;
    }
    function toLayout(i) {
      const X = px[i], Y = py[i], Z = pz[i];
      const x1 = X * cyR + Z * syR, z1 = -X * syR + Z * cyR;
      px[i] = x1;
      py[i] = Y * ctR - z1 * stR;
      pz[i] = Y * stR + z1 * ctR;
      const VX = vx[i], VY = vy[i], VZ = vz[i];
      const vx1 = VX * cyR + VZ * syR, vz1 = -VX * syR + VZ * cyR;
      vx[i] = vx1;
      vy[i] = VY * ctR - vz1 * stR;
      vz[i] = VY * stR + vz1 * ctR;
    }
    function toWorld(i) {
      const X = px[i], Y = py[i], Z = pz[i];
      const y1 = Y * ctR + Z * stR, z1 = -Y * stR + Z * ctR;
      px[i] = X * cyR - z1 * syR;
      py[i] = y1;
      pz[i] = X * syR + z1 * cyR;
      const VX = vx[i], VY = vy[i], VZ = vz[i];
      const vy1 = VY * ctR + VZ * stR, vz1 = -VY * stR + VZ * ctR;
      vx[i] = VX * cyR - vz1 * syR;
      vy[i] = vy1;
      vz[i] = VX * syR + vz1 * cyR;
    }
    const rr = mulberry32(778811);
    let mapOpen = false, mapOpenAt = 0;
    let fireAt, fireDone;
    function stagger(x, y, now) {
      const d = Math.sqrt(x * x + y * y);
      const m = Math.max(1, Math.sqrt(mapHalfW * mapHalfW + mapHalfH * mapHalfH));
      return now + d / m * T.bloomSpread + rr() * T.bloomJitter;
    }
    function recruitNode(ni, now) {
      const n = nodes[ni];
      if (n.parts.length || n.kind === 0) return;
      const tap = n.kind === 1 ? 1 : Math.pow(GROW.depthTaper, n.depth - 2);
      const count = Math.round((n.kind === 1 ? T.nAnchor : T.nChild * tap) * densityK);
      const rad = (n.kind === 1 ? T.orbitAnchor : T.orbitChild * Math.pow(tap, 0.6)) * uiScale;
      const base = stagger(n.x, n.y, now);
      for (let k = 0; k < count; k++) {
        const a = rr() * TAU, e = Math.acos(rr() * 2 - 1);
        const r = rad * Math.pow(rr(), 0.42);
        const lx = n.x + Math.sin(e) * Math.cos(a) * r;
        const ly = n.y + Math.sin(e) * Math.sin(a) * r;
        const i = takeNearestFree(lx, ly);
        if (i < 0) break;
        toLayout(i);
        role[i] = ROLE_TAKEN;
        own[i] = ni;
        ox[i] = Math.sin(e) * Math.cos(a) * r;
        oy[i] = Math.sin(e) * Math.sin(a) * r;
        oz[i] = Math.cos(e) * r * T.orbitFlatten;
        stiff[i] = T.stiffNode[0] + rr() * (T.stiffNode[1] - T.stiffNode[0]);
        damp[i] = T.dampNode;
        t0[i] = base + rr() * 0.22;
        n.parts.push(i);
      }
      n.arriveAt = base + 0.75;
    }
    const _pp = [0, 0], _pt = [0, 0];
    function recruitLink(li, now) {
      const l = links[li];
      if (l.parts.length) return;
      const A = nodes[l.a], B = nodes[l.b];
      const count = clamp(Math.round(l.len / T.linkSpacing * densityK), 4, 44);
      const inset = Math.min(0.26, T.orbitChild * 1.7 * uiScale / Math.max(1, l.len));
      const u0 = inset, u1 = 1 - inset;
      const base = stagger((A.x + B.x) * 0.5, (A.y + B.y) * 0.5, now);
      for (let k = 0; k < count; k++) {
        const u = u0 + (u1 - u0) * (count > 1 ? k / (count - 1) : 0.5) + (rr() - 0.5) * 0.01;
        pathAt(li, u, _pp);
        pathTan(li, u, _pt);
        const j = (rr() - 0.5) * 2 * T.linkJitter * uiScale;
        const nx = -_pt[1] * j, ny = _pt[0] * j;
        const i = takeNearestFree(_pp[0] + nx, _pp[1] + ny);
        if (i < 0) break;
        toLayout(i);
        role[i] = ROLE_TAKEN;
        own[i] = -1 - li;
        upos[i] = u;
        ox[i] = nx;
        oy[i] = ny;
        oz[i] = (rr() - 0.5) * 2 * T.linkJitterZ;
        stiff[i] = T.stiffNode[0] + rr() * (T.stiffNode[1] - T.stiffNode[0]);
        damp[i] = T.dampNode;
        t0[i] = base + u * 0.3 + rr() * 0.14;
        l.parts.push(i);
      }
    }
    function releaseParts(list, now) {
      for (let k = 0; k < list.length; k++) {
        const i = list[k];
        if (role[i] !== ROLE_TAKEN) continue;
        toWorld(i);
        role[i] = ROLE_FREE;
        own[i] = -1;
        stiff[i] = T.stiffFree[0] + rr() * (T.stiffFree[1] - T.stiffFree[0]);
        damp[i] = T.dampFree;
        t0[i] = now + rr() * T.releaseJitter;
      }
      list.length = 0;
    }
    let selected = -1;
    let lastLogoTap = -99, pendingToggle = 0;
    function reconcile(now) {
      for (let i = 1; i < nodes.length; i++)
        if (!nodeVisible(i) && nodes[i].parts.length) {
          releaseParts(nodes[i].parts, now);
          nodes[i].labelA = 0;
        }
      for (let i = 0; i < links.length; i++)
        if (!linkVisible(i) && links[i].parts.length) {
          releaseParts(links[i].parts, now);
          fireAt[i] = -99;
        }
      rebuildFreeGrid();
      for (let i = 1; i < nodes.length; i++) if (nodeVisible(i)) recruitNode(i, now);
      for (let i = 0; i < links.length; i++) if (linkVisible(i)) recruitLink(i, now);
      buildCarve();
    }
    if (typeof opts.autoOpenAfter === "number") T.autoOpenAfter = opts.autoOpenAfter;
    function openMap(now) {
      if (mapOpen) return;
      mapOpen = true;
      mapOpenAt = now;
      reconcile(now);
    }
    function closeMap(now) {
      if (!mapOpen) return;
      mapOpen = false;
      selected = -1;
      reconcile(now);
    }
    function toggleAnchor(ni, now) {
      nodes[ni].expanded = !nodes[ni].expanded;
      reconcile(now);
    }
    function fireLink(li, when) {
      if (!linkVisible(li)) return;
      if (when - fireAt[li] < T.fireTravel * 1.15) return;
      fireAt[li] = when;
      fireDone[li] = 0;
    }
    function fireNode(ni, now) {
      nodes[ni].fireAt = now;
      for (let i = 0; i < links.length; i++)
        if (links[i].a === ni && linkVisible(i)) fireLink(i, now + 0.05);
    }
    function updateFirings(now, breath2, dt) {
      if (!reduced) {
        const p = T.fireRate * dt * (0.1 + breath2 * breath2 * breath2 * 1.35);
        if (Math.random() < p) {
          let tries = 0;
          do {
            const li = Math.random() * links.length | 0;
            if (linkVisible(li)) {
              fireLink(li, now);
              break;
            }
          } while (++tries < 12);
        }
      }
      for (let i = 0; i < links.length; i++) {
        if (fireDone[i] || fireAt[i] < -50) continue;
        if (now - fireAt[i] >= T.fireTravel) {
          fireDone[i] = 1;
          const b = links[i].b;
          nodes[b].fireAt = now;
          if (!reduced && breath2 > 0.72 && Math.random() < T.cascadeChance) {
            let spilled = 0;
            for (let j = 0; j < links.length && spilled < 3; j++) {
              if (links[j].a === b && linkVisible(j)) {
                fireLink(j, now + 0.04 + spilled * 0.07);
                spilled++;
              }
            }
          }
        }
      }
    }
    let camX = 0, camY = 0, zoom = 1, densityK = 1;
    const WB = 14, GB = 8;
    const STYLES = new Array(WB + GB);
    let bList = null;
    const bCount = new Int32Array(WB + GB);
    let themeT = 0, themeTarget = 0, themeBuilt = -1;
    const CUR = {
      bg: "#000000",
      particle: "255,255,255",
      accent: "243,201,63",
      label: "rgba(236,238,242,",
      labelDim: "rgba(176,180,190,",
      hint: "rgba(150,154,164,"
    };
    function mix3(a, b, t) {
      return (a[0] + (b[0] - a[0]) * t | 0) + "," + (a[1] + (b[1] - a[1]) * t | 0) + "," + (a[2] + (b[2] - a[2]) * t | 0);
    }
    function applyTheme(t) {
      const A = THEME.dark, B = THEME.light;
      CUR.bg = "rgb(" + mix3(A.bg, B.bg, t) + ")";
      CUR.particle = mix3(A.particle, B.particle, t);
      CUR.accent = mix3(A.accent, B.accent, t);
      CUR.label = "rgba(" + mix3(A.label, B.label, t) + ",";
      CUR.labelDim = "rgba(" + mix3(A.labelDim, B.labelDim, t) + ",";
      CUR.hint = "rgba(" + mix3(A.hint, B.hint, t) + ",";
      const ak = A.alphaK + (B.alphaK - A.alphaK) * t;
      for (let i = 0; i < WB; i++) STYLES[i] = "rgba(" + CUR.particle + "," + ((i + 0.5) / WB * 0.92 * ak).toFixed(4) + ")";
      for (let i = 0; i < GB; i++) STYLES[WB + i] = "rgba(" + CUR.accent + "," + ((i + 0.6) / GB * 0.96).toFixed(4) + ")";
      themeBuilt = t;
    }
    function initBuckets() {
      bList = new Array(WB + GB);
      for (let b = 0; b < WB + GB; b++) bList[b] = new Int32Array(N);
    }
    let breath = 0.5, logoPulse = 0;
    function frame(now, dt) {
      const dts = clamp(dt * 60, 0.35, 2.2);
      breath = reduced ? 0.5 : 0.5 + 0.5 * Math.sin(now * TAU / T.breathPeriod);
      const gBri = 1 - T.breathBright * 0.5 + T.breathBright * breath;
      const orbitK = 1 - T.breathOrbit * 0.5 + T.breathOrbit * breath;
      yaw += T.yawSpeed * dt;
      tilt = T.tiltBase + sinT(now * T.tiltRate) * T.tiltSwing;
      updateRot();
      updateFirings(now, breath, dt);
      carveAmt += ((mapOpen ? 1 : 0) - carveAmt) * T.carveEase * dts;
      logoPulse *= Math.pow(0.9, dts);
      if (pendingToggle > 0 && now >= pendingToggle) {
        pendingToggle = 0;
        if (mapOpen) closeMap(now);
        else openMap(now);
      }
      if (themeT !== themeTarget) {
        themeT += (themeTarget - themeT) * T.themeEase * dts;
        if (Math.abs(themeTarget - themeT) < 2e-3) {
          themeT = themeTarget;
          document.documentElement.style.background = CUR.bg;
        }
      }
      if (themeBuilt < 0 || Math.abs(themeT - themeBuilt) > 3e-3) applyTheme(themeT);
      const FOV = T.fov, halfW = cw * 0.5, halfH = ch * 0.5, Zd = zoom * dpr;
      const drift = T.driftAmp * (reduced ? 0.45 : 1);
      bCount.fill(0);
      for (let i = 0; i < N; i++) {
        let tX, tY, tZ, isFree = role[i] === ROLE_FREE;
        if (isFree) {
          const dr = drt[i] * now;
          tX = hx[i] + sinT(dr + dphA[i]) * drift;
          tY = hy[i] + sinT(dr * 0.83 + dphB[i]) * drift;
          tZ = hz[i] + sinT(dr * 1.19 + dphC[i]) * drift;
        } else {
          const o = own[i];
          if (o >= 0) {
            tX = nodeX[o] + ox[i] * orbitK;
            tY = nodeY[o] + oy[i] * orbitK;
            tZ = oz[i] * orbitK;
          } else {
            const li = -1 - o, m = lN[li], f = upos[i] * (m - 1);
            let g = f | 0;
            if (g < 0) g = 0;
            else if (g > m - 2) g = m - 2;
            const w = f - g, b2 = (lOff[li] + g) * 2;
            tX = lPts[b2] + (lPts[b2 + 2] - lPts[b2]) * w + ox[i];
            tY = lPts[b2 + 1] + (lPts[b2 + 3] - lPts[b2 + 1]) * w + oy[i];
            tZ = oz[i];
          }
        }
        if (now >= t0[i]) {
          const k = stiff[i] * dts, d = 1 - (1 - damp[i]) * dts;
          let a;
          a = vx[i] + (tX - px[i]) * k;
          vx[i] = a * d;
          px[i] += a * dts;
          a = vy[i] + (tY - py[i]) * k;
          vy[i] = a * d;
          py[i] += a * dts;
          a = vz[i] + (tZ - pz[i]) * k;
          vz[i] = a * d;
          pz[i] += a * dts;
        } else {
          const d = 1 - 0.06 * dts;
          px[i] += vx[i] * dts;
          py[i] += vy[i] * dts;
          pz[i] += vz[i] * dts;
          vx[i] *= d;
          vy[i] *= d;
          vz[i] *= d;
        }
        let X, Y, Z;
        if (isFree) {
          const x1 = px[i] * cyR + pz[i] * syR, z1 = -px[i] * syR + pz[i] * cyR;
          X = x1;
          Y = py[i] * ctR - z1 * stR;
          Z = py[i] * stR + z1 * ctR;
        } else {
          X = px[i];
          Y = py[i];
          Z = pz[i];
        }
        const s = FOV / (FOV + Z);
        X *= s;
        Y *= s;
        if (isFree && carveAmt > 2e-3) {
          const fx = (X - cgX0) * cgSX, fy = (Y - cgY0) * cgSY;
          if (fx >= 0 && fy >= 0 && fx < CG - 1 && fy < CG - 1) {
            const ix = fx | 0, iy = fy | 0, ux = fx - ix, uy = fy - iy;
            const i00 = iy * CG + ix, i10 = i00 + 1, i01 = i00 + CG, i11 = i01 + 1;
            X += ((carveU[i00] + (carveU[i10] - carveU[i00]) * ux) * (1 - uy) + (carveU[i01] + (carveU[i11] - carveU[i01]) * ux) * uy) * carveAmt;
            Y += ((carveV[i00] + (carveV[i10] - carveV[i00]) * ux) * (1 - uy) + (carveV[i01] + (carveV[i11] - carveV[i01]) * ux) * uy) * carveAmt;
          }
        }
        const SX = (X - camX) * Zd + halfW;
        const SY = (Y - camY) * Zd + halfH;
        if (SX < -6 || SX > cw + 6 || SY < -6 || SY > ch + 6) continue;
        let lum = bri[i] * (0.3 + 0.78 * s) * gBri;
        lum *= 0.8 + 0.2 * sinT(now * twr[i] + twp[i]);
        let e = 0;
        if (!isFree) {
          const o = own[i];
          if (o >= 0) {
            lum *= 1.22;
            const n = nodes[o];
            const ag = (now - n.fireAt) / T.fireNodeDecay;
            if (ag >= 0 && ag < 1) {
              const w = 1 - ag;
              e = w * w;
            }
            if (o === selected) e = Math.max(e, 0.52 + 0.16 * breath);
          } else {
            lum *= 1.12;
            const li = -1 - o, d = (now - fireAt[li]) / T.fireTravel - upos[i];
            if (d > -0.06 && d < T.fireWidth) {
              const w = d < 0 ? 1 + d / 0.06 : 1 - d / T.fireWidth;
              e = w * w;
            }
          }
        }
        let sz = szb[i] * (0.62 + 0.8 * s);
        let b;
        if (e > 0.1) {
          lum = clamp(lum * (0.55 + 0.75 * e) + e * 0.55, 0, 1);
          sz *= 1 + 0.85 * e;
          b = WB + clamp(lum * GB | 0, 0, GB - 1);
        } else {
          lum = clamp(lum, 0, 1);
          b = clamp(lum * WB | 0, 0, WB - 1);
        }
        sxp[i] = SX;
        syp[i] = SY;
        ssz[i] = Math.max(0.8, sz * dpr);
        bList[b][bCount[b]++] = i;
      }
      ctx.fillStyle = CUR.bg;
      ctx.fillRect(0, 0, cw, ch);
      for (let b = 0; b < WB + GB; b++) {
        const n = bCount[b];
        if (!n) continue;
        ctx.fillStyle = STYLES[b];
        const L = bList[b];
        for (let k = 0; k < n; k++) {
          const i = L[k], z = ssz[i], h = z * 0.5;
          ctx.fillRect(sxp[i] - h, syp[i] - h, z, z);
        }
      }
      drawLabels(now, dts);
      drawLogo();
    }
    const FONT = '-apple-system,BlinkMacSystemFont,"Segoe UI",Inter,Helvetica,Arial,sans-serif';
    function drawLabels(now, dts) {
      ctx.textAlign = "center";
      ctx.textBaseline = "middle";
      const Zd = zoom * dpr, halfW = cw * 0.5, halfH = ch * 0.5;
      const aSize = (isSmall ? 10.5 : 11.5) * dpr;
      const cSize = (isSmall ? 9.5 : 10.5) * dpr;
      for (let i = 1; i < nodes.length; i++) {
        const n = nodes[i];
        const vis = nodeVisible(i);
        const want = vis && now > n.arriveAt ? 1 : 0;
        n.labelA += (want - n.labelA) * 0.055 * dts;
        if (n.labelA < 0.01) continue;
        const SX = (n.x - camX) * Zd + halfW;
        const SY = (n.y - camY) * Zd + halfH;
        if (SX < -140 || SX > cw + 140 || SY < -40 || SY > ch + 40) continue;
        const anchor = n.kind === 1;
        let a = n.labelA * (anchor ? 0.92 : 0.68);
        const gold = i === selected || now - n.fireAt < T.fireNodeDecay * 0.9;
        if (gold) a = Math.min(1, a + 0.22);
        let tx, ty;
        if (anchor) {
          ctx.textAlign = "center";
          tx = SX;
          ty = SY + 26 * dpr;
        } else if (Math.abs(n.lox) > 0.58) {
          ctx.textAlign = n.lox > 0 ? "left" : "right";
          tx = SX + (n.lox > 0 ? 13 : -13) * dpr;
          ty = SY + 0.5 * dpr;
        } else {
          ctx.textAlign = "center";
          tx = SX;
          ty = SY + (n.loy >= 0 ? 18 : -17) * dpr;
        }
        ctx.font = (anchor ? "600 " : "400 ") + (anchor ? aSize : cSize).toFixed(1) + "px " + FONT;
        if ("letterSpacing" in ctx) ctx.letterSpacing = anchor ? 1.2 * dpr + "px" : 0.3 * dpr + "px";
        ctx.fillStyle = gold ? "rgba(" + CUR.accent + "," + a.toFixed(3) + ")" : (anchor ? CUR.label : CUR.labelDim) + a.toFixed(3) + ")";
        ctx.fillText(anchor ? n.label.toUpperCase() : n.label, tx, ty);
        if (anchor && n.hasKids && !n.expanded && n.labelA > 0.3) {
          ctx.fillStyle = CUR.labelDim + (n.labelA * 0.34).toFixed(3) + ")";
          ctx.fillRect(SX - 5 * dpr, SY + 35 * dpr, 10 * dpr, Math.max(1, dpr * 0.8));
        }
      }
      if ("letterSpacing" in ctx) ctx.letterSpacing = "0px";
    }
    let logoX = 0, logoY = 0, logoR = 0;
    function drawLogo() {
      const Zd = zoom * dpr;
      const size = (isSmall ? 70 : 86) * dpr * (1 + logoPulse * 0.1);
      logoX = (0 - camX) * Zd + cw * 0.5;
      logoY = (0 - camY) * Zd + ch * 0.5;
      logoR = size * 0.62;
      ctx.fillStyle = "rgba(" + CUR.particle + ",0.97)";
      ctx.beginPath();
      for (let t = 0; t < LOGO.length; t++) {
        const tri = LOGO[t];
        ctx.moveTo(logoX + tri[0][0] * size, logoY + tri[0][1] * size);
        ctx.lineTo(logoX + tri[1][0] * size, logoY + tri[1][1] * size);
        ctx.lineTo(logoX + tri[2][0] * size, logoY + tri[2][1] * size);
        ctx.closePath();
      }
      ctx.fill();
    }
    let userZoomed = false;
    function fitZoom() {
      let open = false;
      for (let i = 1; i < nodes.length; i++) if (nodes[i].kind === 1 && nodes[i].expanded) {
        open = true;
        break;
      }
      const k = open ? 1 : 0.8;
      const fw = Math.max(ancHalfW, mapHalfW * k);
      const fh = Math.max(ancHalfH, mapHalfH * k);
      zoom = clamp(Math.min((vw * 0.5 - 18) / fw, (vh * 0.5 - 26) / fh), T.zoomRange[0], 1.25);
      camX = 0;
      camY = 0;
    }
    function resize() {
      const wasPortrait = isPortrait;
      const _r = stageRect();
      vw = _r.w;
      vh = _r.h;
      dpr = Math.min(2, window.devicePixelRatio || 1);
      cw = Math.round(vw * dpr);
      ch = Math.round(vh * dpr);
      cv.width = cw;
      cv.height = ch;
      cv.style.width = vw + "px";
      cv.style.height = vh + "px";
      isPortrait = vh >= vw;
      isSmall = Math.min(vw, vh) < 620;
      const L = isPortrait ? LAYOUT.portrait : LAYOUT.landscape;
      const S = Math.min(Math.min(vw * L.sx, L.cx), Math.min(vh * L.sy, L.cy));
      uiScale = clamp(S / 380, 0.7, 1.15);
      buildGeometry();
      buildCarve();
      if (!userZoomed || wasPortrait !== isPortrait) fitZoom();
      const hwL = vw * 0.5 / zoom, hhL = vh * 0.5 / zoom;
      const kx = clamp(hwL / T.cloudRadius, 0.35, 2.4);
      const ky = clamp(hhL / T.cloudRadius, 0.35, 2.4);
      applyCloudScale(kx, ky, Math.min(kx, ky));
      clampCam();
    }
    function clampCam() {
      const lim = T.panLimit;
      camX = clamp(camX, -lim, lim);
      camY = clamp(camY, -lim, lim);
    }
    const ptrs = /* @__PURE__ */ new Map();
    let dragging = false, moved = 0, downX = 0, downY = 0, downT = 0;
    let lastX = 0, lastY = 0;
    let pinchD = 0, pinchZ = 1, pinchUX = 0, pinchUY = 0;
    function midOf() {
      let sx = 0, sy = 0, n = 0;
      ptrs.forEach(function(p) {
        sx += p.x;
        sy += p.y;
        n++;
      });
      return [sx / n, sy / n];
    }
    function distOf() {
      const it = ptrs.values();
      const a = it.next().value, b = it.next().value;
      return Math.hypot(a.x - b.x, a.y - b.y);
    }
    function zoomAt(nz, sx, sy) {
      nz = clamp(nz, T.zoomRange[0], T.zoomRange[1]);
      const ux = (sx - vw * 0.5) / zoom + camX;
      const uy = (sy - vh * 0.5) / zoom + camY;
      zoom = nz;
      camX = ux - (sx - vw * 0.5) / zoom;
      camY = uy - (sy - vh * 0.5) / zoom;
      userZoomed = true;
      clampCam();
    }
    on(cv, "pointerdown", function(e) {
      try {
        cv.setPointerCapture(e.pointerId);
      } catch (_) {
      }
      ptrs.set(e.pointerId, { x: e.clientX, y: e.clientY });
      if (ptrs.size === 1) {
        dragging = true;
        moved = 0;
        downX = lastX = e.clientX;
        downY = lastY = e.clientY;
        downT = performance.now() / 1e3;
        cv.classList.add("dragging");
      } else if (ptrs.size === 2) {
        dragging = false;
        cv.classList.remove("dragging");
        pinchD = distOf() || 1;
        pinchZ = zoom;
        const m = midOf();
        pinchUX = (m[0] - vw * 0.5) / zoom + camX;
        pinchUY = (m[1] - vh * 0.5) / zoom + camY;
      }
    });
    on(cv, "pointermove", function(e) {
      const p = ptrs.get(e.pointerId);
      if (!p) return;
      p.x = e.clientX;
      p.y = e.clientY;
      if (ptrs.size === 1 && dragging) {
        const dx = e.clientX - lastX, dy = e.clientY - lastY;
        lastX = e.clientX;
        lastY = e.clientY;
        moved += Math.abs(dx) + Math.abs(dy);
        camX -= dx / zoom;
        camY -= dy / zoom;
        userZoomed = true;
        clampCam();
      } else if (ptrs.size === 2) {
        const d = distOf() || 1;
        const m = midOf();
        zoom = clamp(pinchZ * (d / pinchD), T.zoomRange[0], T.zoomRange[1]);
        camX = pinchUX - (m[0] - vw * 0.5) / zoom;
        camY = pinchUY - (m[1] - vh * 0.5) / zoom;
        userZoomed = true;
        clampCam();
      }
    });
    function pointerEnd(e) {
      const was = ptrs.size;
      ptrs.delete(e.pointerId);
      cv.classList.remove("dragging");
      if (was === 1 && dragging) {
        if (moved < T.tapSlop && performance.now() / 1e3 - downT < 0.7)
          handleTap(downX, downY, clock);
      }
      if (ptrs.size === 1) {
        const it = ptrs.values().next().value;
        dragging = true;
        moved = 999;
        lastX = it.x;
        lastY = it.y;
      } else if (ptrs.size === 0) {
        dragging = false;
      }
    }
    on(cv, "pointerup", pointerEnd);
    on(cv, "pointercancel", pointerEnd);
    on(cv, "wheel", function(e) {
      e.preventDefault();
      const k = Math.exp(-e.deltaY * (e.deltaMode === 1 ? 0.028 : 16e-4));
      zoomAt(zoom * k, e.clientX, e.clientY);
    }, { passive: false });
    function nearestNode(x, y, reach) {
      let best = -1, bestD = reach * reach;
      for (let i = 1; i < nodes.length; i++) {
        if (!nodeVisible(i)) continue;
        const dx = x - ((nodes[i].x - camX) * zoom + vw * 0.5);
        const dy = y - ((nodes[i].y - camY) * zoom + vh * 0.5);
        const d = dx * dx + dy * dy;
        if (d < bestD) {
          bestD = d;
          best = i;
        }
      }
      return best;
    }
    function handleTap(x, y, now) {
      const reach = isSmall ? 34 : 30;
      const near = nearestNode(x, y, reach);
      const ldx = x * dpr - logoX, ldy = y * dpr - logoY;
      const overLogo = ldx * ldx + ldy * ldy < (logoR + 12 * dpr) * (logoR + 12 * dpr);
      if (overLogo && near < 0) {
        logoPulse = 1;
        if (now - lastLogoTap < T.doubleTap) {
          lastLogoTap = -99;
          pendingToggle = 0;
          themeTarget = 1 - themeTarget;
        } else {
          lastLogoTap = now;
          pendingToggle = now + T.doubleTap;
        }
        return;
      }
      if (!mapOpen) {
        selected = -1;
        return;
      }
      const best = near;
      if (best < 0) {
        selected = -1;
        return;
      }
      selected = best;
      fireNode(best, now);
      if (nodes[best].kind === 1 && nodes[best].hasKids) toggleAnchor(best, now);
      onNodeTap(best);
    }
    let raf = 0, last = 0, running = false, clock = 0;
    function tick(ts) {
      raf = requestAnimationFrame(tick);
      const t = ts / 1e3;
      let dt = last ? t - last : 1 / 60;
      last = t;
      if (dt > 0.05) dt = 0.05;
      clock += dt;
      frame(clock, dt);
      if (!mapOpen && !autoDone && clock > T.autoOpenAfter) {
        autoDone = true;
        openMap(clock);
      }
    }
    let autoDone = false;
    function start() {
      if (running) return;
      running = true;
      last = 0;
      raf = requestAnimationFrame(tick);
    }
    function stop() {
      running = false;
      cancelAnimationFrame(raf);
    }
    on(document, "visibilitychange", function() {
      if (document.hidden) stop();
      else start();
    });
    on(window, "resize", resize);
    on(window, "orientationchange", function() {
      setTimeout(resize, 120);
    });
    function flatData() {
      const out = [null];
      for (let c = 0; c < MAP.length; c++) {
        (function walk(node) {
          out.push(node);
          const kids = node.children || [];
          for (let k = 0; k < kids.length; k++) walk(kids[k]);
        })(MAP[c]);
      }
      return out;
    }
    const DATA = flatData();
    function goTo(d) {
      if (!d) return;
      if (typeof opts.onNodeOpen === "function" && opts.onNodeOpen(d) === false) return;
      if (!d.href) return;
      if (/^mailto:/i.test(d.href)) {
        window.location.href = d.href;
        return;
      }
      const a = document.createElement("a");
      a.href = d.href;
      a.target = "_blank";
      a.rel = "noopener";
      document.body.appendChild(a);
      a.click();
      a.remove();
    }
    function onNodeTap(i) {
      goTo(DATA[i]);
    }
    function zoomStep(k) {
      zoomAt(zoom * k, vw * 0.5, vh * 0.5);
    }
    function home() {
      camX = 0;
      camY = 0;
      zoom = 1;
      userZoomed = false;
      clampCam();
      resize();
    }
    const els = opts.elements || {};
    on(els.zoomIn, "click", function() {
      zoomStep(1.25);
    });
    on(els.zoomOut, "click", function() {
      zoomStep(1 / 1.25);
    });
    on(els.reset, "click", home);
    const indexView = els.indexPanel || null;
    function showIndex(on_) {
      if (!indexView) return;
      indexView.classList.toggle("is-on", on_);
      if (on_) {
        const first = indexView.querySelector("a");
        if (first) first.focus();
      } else if (els.indexOpen) {
        els.indexOpen.focus();
      }
      if (typeof opts.onIndexToggle === "function") opts.onIndexToggle(on_);
    }
    on(els.indexOpen, "click", function() {
      showIndex(true);
    });
    on(els.indexClose, "click", function() {
      showIndex(false);
    });
    on(els.skipLink, "click", function() {
      showIndex(true);
    });
    on(document, "keydown", function(e) {
      if (e.key !== "Escape") return;
      if (indexView && indexView.classList.contains("is-on")) showIndex(false);
    });
    (function boot() {
      const _r = stageRect();
      vw = _r.w;
      vh = _r.h;
      isSmall = Math.min(vw, vh) < 620;
      densityK = isSmall ? 0.78 : 1;
      allocParticles(isSmall ? T.countMobile : T.countDesktop);
      buildCloud();
      initFreeGrid();
      initBuckets();
      applyTheme(0);
      resize();
      const pre = typeof opts.preroll === "number" ? opts.preroll : 0;
      if (pre > 0) {
        const step = 1 / 60;
        autoDone = true;
        openMap(0);
        for (let t = 0; t < pre; t += step) {
          clock = t;
          frame(t, step);
        }
      }
      start();
    })();
    let ro = null;
    if (typeof ResizeObserver !== "undefined") {
      let last2 = stageRect();
      ro = new ResizeObserver(function() {
        const r = stageRect();
        if (r.w === last2.w && r.h === last2.h) return;
        last2 = r;
        resize();
      });
      ro.observe(canvas);
    }
    return function dispose() {
      stop();
      if (ro) ro.disconnect();
      for (let i = 0; i < _off.length; i++) _off[i]();
      _off.length = 0;
    };
  }
  function MindMap({ map, elements, onNodeOpen, autoOpenAfter, preroll, ariaLabel, className }) {
    const ref = React.useRef(null);
    React.useEffect(() => {
      if (!ref.current) return;
      const dispose = startMindMap(ref.current, { map, elements, onNodeOpen, autoOpenAfter, preroll });
      return () => {
        if (typeof dispose === "function") dispose();
      };
    }, [map, elements, onNodeOpen, autoOpenAfter, preroll]);
    return /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
      "canvas",
      {
        ref,
        className: ["map-stage__canvas", className].filter(Boolean).join(" "),
        role: "img",
        "aria-label": ariaLabel || "Interactive mind map of this site. Tap the mark to open it, then tap a node to open it."
      }
    );
  }
  function HudBrand({
    lines = ["Xavier", "de", "Belle"],
    strapline,
    className
  }) {
    const head = lines.slice(0, -1);
    const tail = lines[lines.length - 1];
    return /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("header", { className: ["hud", "hud--brand", className].filter(Boolean).join(" "), children: [
      /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("h1", { children: [
        head.map((l, i) => /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("i", { children: l }, i)),
        tail ? /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("em", { children: tail }) : null
      ] }),
      strapline ? /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("p", { children: strapline }) : null
    ] });
  }
  function HudShortcuts({ links = [], className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
      "nav",
      {
        className: ["hud", "hud--top", className].filter(Boolean).join(" "),
        "aria-label": "Shortcuts",
        children: links.map((l) => /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
          "a",
          {
            href: l.href,
            target: l.external ? "_blank" : void 0,
            rel: l.external ? "noopener" : void 0,
            children: l.label
          },
          l.href + l.label
        ))
      }
    );
  }
  function HudHint({ children, className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
      "div",
      {
        className: ["hud", "hud--hint", className].filter(Boolean).join(" "),
        "aria-hidden": "true",
        children
      }
    );
  }
  function HudControls({ children, className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className: ["hud", "hud--ctl", className].filter(Boolean).join(" "), children });
  }
  function IndexPanel({
    open = false,
    title,
    kicker,
    columns = [],
    footer,
    className
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)(
      "section",
      {
        className: ["index", open ? "is-on" : "", className].filter(Boolean).join(" "),
        "aria-label": "Text index of the whole site",
        children: [
          title ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(Display, { as: "h2", children: title }) : null,
          kicker ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(Kicker, { children: kicker }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "index__grid", children: columns.map((col) => /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "index__col", children: [
            /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("h3", { children: col.heading }),
            col.links.map((l) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
              "a",
              {
                href: l.href,
                target: l.external ? "_blank" : void 0,
                rel: l.external ? "noopener" : void 0,
                children: l.label
              },
              l.href + l.label
            ))
          ] }, col.heading)) }),
          footer ? /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("p", { style: { marginTop: 34 }, children: footer }) : null
        ]
      }
    );
  }
  function Masthead({ kicker, title, sub, emphasis = "volt", className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)(
      "section",
      {
        className: ["mast", emphasis === "stroke" ? "mast--stroke" : "", className].filter(Boolean).join(" "),
        children: [
          kicker ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(Kicker, { children: kicker }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("h1", { className: "display", children: title }),
          sub ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("p", { className: "mast__sub", children: sub }) : null
        ]
      }
    );
  }
  function Marquee({ items = [], className }) {
    const run = items.concat(items);
    return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("div", { className: ["strip", className].filter(Boolean).join(" "), "aria-hidden": "true", children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("div", { className: "strip__run", children: run.map((t, i) => /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("span", { children: t }, i)) }) });
  }
  function Chapter({
    id,
    eyebrow,
    title,
    role,
    children,
    aside,
    tone = "ink",
    className
  }) {
    const cls = [
      "chapter",
      tone === "volt" ? "chapter--volt" : "",
      tone === "paper" ? "chapter--paper" : "",
      className
    ].filter(Boolean).join(" ");
    return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("section", { className: cls, id, children: [
      eyebrow ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("p", { className: "chapter__no", children: eyebrow }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("h2", { className: "display", children: title }),
      role ? /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("p", { className: "chapter__role", children: role }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "chapter__cols", children: [
        /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("div", { children }),
        aside
      ] })
    ] });
  }
  function SpecList({ items = [], className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("ul", { className: ["spec", className].filter(Boolean).join(" "), children: items.map((it, i) => /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("li", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("b", { children: it.term }),
      /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("span", { children: it.detail })
    ] }, i)) });
  }
  function ActionRow({ children, className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("div", { className: ["acts", className].filter(Boolean).join(" "), children });
  }
  function ClosingCall({ kicker, title, children, actions, className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime22.jsxs)("section", { className: ["call", className].filter(Boolean).join(" "), children: [
      kicker ? /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Kicker, { children: kicker }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime22.jsx)("h2", { className: "display", children: title }),
      children ? /* @__PURE__ */ (0, import_jsx_runtime22.jsx)("p", { children }) : null,
      actions
    ] });
  }
  function FilterRail({
    children,
    search = true,
    searchPlaceholder = "Search tools",
    searchValue,
    onSearchChange,
    className
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime23.jsxs)("div", { className: ["rail", className].filter(Boolean).join(" "), children: [
      /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("div", { className: "rail__chips", children }),
      search ? /* @__PURE__ */ (0, import_jsx_runtime23.jsxs)("label", { className: "rail__search", children: [
        /* @__PURE__ */ (0, import_jsx_runtime23.jsxs)("svg", { viewBox: "0 0 24 24", "aria-hidden": "true", children: [
          /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("circle", { cx: "11", cy: "11", r: "7" }),
          /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("line", { x1: "16.5", y1: "16.5", x2: "21", y2: "21" })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
          "input",
          {
            type: "search",
            placeholder: searchPlaceholder,
            value: searchValue,
            onChange: (e) => onSearchChange && onSearchChange(e.target.value),
            "aria-label": searchPlaceholder
          }
        )
      ] }) : null
    ] });
  }
  function Band({ id, no, title, blurb, children, className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)("section", { className: ["band", className].filter(Boolean).join(" "), id, children: [
      /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)("div", { className: "band__head", children: [
        no ? /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("span", { className: "band__no", children: no }) : null,
        /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("h2", { className: "display", children: title }),
        blurb ? /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("p", { children: blurb }) : null
      ] }),
      children
    ] });
  }
  function CardGrid({ children, className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("div", { className: ["cards", className].filter(Boolean).join(" "), children });
  }
  function ToolCard({
    no,
    title,
    children,
    tags = [],
    href,
    external = true,
    cta = "Open Tool",
    className
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)(
      "a",
      {
        className: ["card", className].filter(Boolean).join(" "),
        href,
        target: external ? "_blank" : void 0,
        rel: external ? "noopener" : void 0,
        children: [
          no ? /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { className: "card__no", children: no }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("h3", { children: title }),
          /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("p", { children }),
          tags.length ? /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("div", { className: "card__tags", children: tags.map((t) => /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("i", { children: t }, t)) }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)("span", { className: "card__go", children: [
            cta,
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { children: "\u2197" }),
            external ? /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { className: "sr-only", children: "(opens in a new tab)" }) : null
          ] })
        ]
      }
    );
  }
  function EmptyState({ children = "Nothing matches that.", className }) {
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("div", { className: ["empty", className].filter(Boolean).join(" "), children: /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("p", { children }) });
  }
  return __toCommonJS(index_es_exports);
})();
window.XDB=XDB.__dsMainNs?Object.assign({},XDB,XDB.__dsMainNs,{__dsMainNs:undefined}):XDB;
